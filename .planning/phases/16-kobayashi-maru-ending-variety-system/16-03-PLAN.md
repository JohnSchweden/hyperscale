---
phase: 16-kobayashi-maru-ending-variety-system
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - data/cards/head-of-something.ts
  - data/cards/chief-something-officer.ts
  - data/cards/something-manager.ts
  - data/cards/tech-ai-consultant.ts
  - data/cards/data-scientist.ts
  - data/cards/software-architect.ts
  - data/cards/software-engineer.ts
  - data/cards/vibe-coder.ts
  - data/cards/vibe-engineer.ts
  - data/cards/agentic-engineer.ts
  - unit/deathVectorCoverage.test.ts
autonomous: true
requirements:
  - DV-05
  - DV-06

must_haves:
  truths:
    - "Every card deck has death vectors distributed across at least 4 of 6 non-KIRK death types"
    - "Congressional hearing content exists — CONGRESS death type can actually trigger"
    - "Death vector distribution is balanced enough that different play styles lead to different endings"
  artifacts:
    - path: "unit/deathVectorCoverage.test.ts"
      provides: "Validation that all 10 decks have balanced death vector coverage"
    - path: "data/cards/head-of-something.ts"
      provides: "HoS cards with deathVector annotations on outcomes"
  key_links:
    - from: "data/cards/*.ts"
      to: "types.ts"
      via: "Card outcome deathVector field"
      pattern: "deathVector.*DeathType"
---

<objective>
Add death vector metadata to existing card outcomes and fill the CONGRESS content gap.

Purpose: Without death vectors on cards, the new logic from Plans 01-02 falls back to legacy behavior. This plan annotates existing cards so player choices actually drive varied endings. Also adds 2-3 congressional hearing cards to fill the CONGRESS content gap.
Output: All 10 card decks annotated with death vectors, CONGRESS content gap filled, coverage validation tests.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-kobayashi-maru-ending-variety-system/16-CONTEXT.md
@.planning/phases/16-kobayashi-maru-ending-variety-system/16-01-SUMMARY.md

<interfaces>
From types.ts (modified in Plan 01):
```typescript
// Card outcome now has optional deathVector
onRight: {
  label: string;
  hype: number;
  heat: number;
  fine: number;
  violation: string;
  feedback: { [key in PersonalityType]: string };
  lesson: string;
  deathVector?: DeathType; // NEW — which death type this outcome pushes toward
};
```

Death type to outcome thematic mapping (use as annotation guide):
- BANKRUPT: Outcomes involving massive fines, budget destruction, financial ruin
- PRISON: Outcomes involving legal violations, regulatory fraud, cover-ups, compliance evasion
- CONGRESS: Outcomes involving public exposure, congressional scrutiny, whistleblower scenarios, media attention
- FLED_COUNTRY: Outcomes involving international incidents, cross-border data breaches, global reputational damage
- REPLACED_BY_SCRIPT: Outcomes involving automation replacing human judgment, over-reliance on AI, job obsolescence
- AUDIT_FAILURE: Outcomes involving documentation gaps, process failures, governance breakdowns
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write death vector coverage validation test</name>
  <files>unit/deathVectorCoverage.test.ts</files>
  <action>
    Create a validation test file that will verify death vector coverage AFTER Task 2 annotates the cards. Write this first so it defines the contract.

    Tests to write:
    1. Each of 10 role decks has at least 40% of card outcomes with deathVector annotations (not all outcomes need vectors — some cards are neutral)
    2. Each deck covers at least 4 of the 6 non-KIRK death types across its outcomes
    3. No single death type makes up more than 40% of any deck's vectors (prevents new single-type dominance)
    4. CONGRESS death type appears in at least 3 different decks (fills the content gap)
    5. Total across all decks: each of 6 non-KIRK death types has at least 5 occurrences

    Import all 10 role card arrays and ROLE_CARDS from data/cards. Iterate through each deck, check onLeft and onRight deathVector fields.

    This test should FAIL initially (RED) since no cards have vectors yet.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run test:unit -- --filter=deathVectorCoverage 2>&1 | tail -5</automated>
  </verify>
  <done>Coverage validation test exists and fails (RED phase — no vectors on cards yet)</done>
</task>

<task type="auto">
  <name>Task 2: Annotate card outcomes with death vectors and add CONGRESS cards</name>
  <files>data/cards/head-of-something.ts, data/cards/chief-something-officer.ts, data/cards/something-manager.ts, data/cards/tech-ai-consultant.ts, data/cards/data-scientist.ts, data/cards/software-architect.ts, data/cards/software-engineer.ts, data/cards/vibe-coder.ts, data/cards/vibe-engineer.ts, data/cards/agentic-engineer.ts</files>
  <action>
    For each of the 10 role card decks, add `deathVector` to outcome objects based on the thematic content of each outcome. Guidelines:

    **Annotation rules (outcome-level, not card-level):**
    - Only annotate outcomes where the consequence clearly maps to a death type
    - Leave neutral/ambiguous outcomes WITHOUT a deathVector (the field is optional)
    - The SAME card can have different deathVectors on left vs right (different consequences)
    - Target: ~50-60% of outcomes annotated, rest left neutral
    - Do NOT add deathVector to Kirk corrupted cards (those are handled separately)

    **Thematic mapping:**
    - `DeathType.BANKRUPT`: Outcomes with massive financial penalties, budget hemorrhaging, investor pullout
    - `DeathType.PRISON`: Outcomes involving cover-ups, regulatory fraud, compliance evasion, data deletion
    - `DeathType.CONGRESS`: Outcomes involving public testimony, congressional scrutiny, whistleblower exposure, media firestorms
    - `DeathType.FLED_COUNTRY`: Outcomes involving international data breaches, cross-border incidents, global PR disasters
    - `DeathType.REPLACED_BY_SCRIPT`: Outcomes where AI replaces human judgment, automation makes the role obsolete, over-delegation to AI
    - `DeathType.AUDIT_FAILURE`: Outcomes with documentation gaps, process breakdowns, internal audit triggers

    **CONGRESS content gap fix:**
    Add 2-3 new cards to the `head-of-something.ts` and `chief-something-officer.ts` decks specifically themed around congressional/regulatory testimony scenarios. These cards should have CONGRESS deathVector on at least one outcome. Follow existing card format exactly, with:
    - Unique id (e.g., `hos_congressional_testimony_1`)
    - AppSource.EMAIL or AppSource.SLACK
    - storyContext for immersion
    - realWorldReference from 2024-2025 AI governance events (e.g., Sam Altman Senate testimony, EU AI Act hearings)
    - 3-personality feedback
    - Both outcomes should have consequences (no-win style)

    **Priority roles for annotation depth:**
    - HEAD_OF_SOMETHING: Most critical (currently 70%+ AUDIT_FAILURE). Ensure broad vector distribution.
    - CHIEF_SOMETHING_OFFICER: Second priority. Spread across CONGRESS, PRISON, BANKRUPT heavily.
    - All other roles: Moderate annotation, ensure at least 4 death types represented.

    Import `DeathType` from `../../types` in each card file (if not already imported).
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run test:unit -- --filter=deathVectorCoverage && bun run typecheck</automated>
  </verify>
  <done>
    - All 10 decks have death vector annotations on ~50-60% of outcomes
    - Each deck covers at least 4 death types
    - CONGRESS content gap filled with 2-3 new cards
    - Coverage validation tests pass (GREEN)
    - Typecheck passes
  </done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- `bun run test:unit -- --filter=deathVectorCoverage` — all coverage tests pass
- `bun run test:unit` — no regressions in any unit tests
- `bun run test:data` — existing card validation tests still pass
</verification>

<success_criteria>
- All 10 decks annotated with death vectors
- CONGRESS death type is actually reachable (content exists)
- No single death type dominates any deck (max 40%)
- Different play styles (always picking risky vs safe options) lead to measurably different death vector accumulations
</success_criteria>

<output>
After completion, create `.planning/phases/16-kobayashi-maru-ending-variety-system/16-03-SUMMARY.md`
</output>
