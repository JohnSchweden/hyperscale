---
phase: 03-no-win-scenario-cards
plan: 06
type: execute
wave: 6
depends_on: ["03-05-revised"]
files_modified: [
  "data/cards/chief-something-officer.ts",
  "data/cards/head-of-something.ts",
  "data/cards/something-manager.ts",
  "data/cards/tech-ai-consultant.ts",
  "data/cards/data-scientist.ts",
  "data/cards/software-architect.ts",
  "data/cards/software-engineer.ts",
  "data/cards/vibe-coder.ts",
  "data/cards/vibe-engineer.ts",
  "data/cards/agentic-engineer.ts",
  "types.ts",
  "hooks/useGameState.ts",
  "data/roles.ts"
]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Each role has fines appropriate to their real-world accountability level"
    - "C-suite roles face $5M-$500M fines (board-level decisions)"
    - "Management roles face $1M-$50M fines (department decisions)"
    - "Technical leads face $500K-$25M fines (architecture/consulting)"
    - "Individual contributors face $100K-$20M fines (code/model decisions)"
    - "Starting budget scales with role responsibility ($40M-$200M)"
    - "Players can experience 8-10 cards before game over"
  artifacts:
    - path: "data/cards/*.ts"
      provides: "Role-appropriate fine values"
    - path: "types.ts"
      provides: "Role budget configuration type"
    - path: "data/roles.ts"
      provides: "Per-role starting budget constants"
    - path: "hooks/useGameState.ts"
      provides: "Dynamic budget initialization per role"
  key_links:
    - from: "data/roles.ts"
      to: "hooks/useGameState.ts"
      via: "Budget lookup on game start"
---

<objective>
Rebalance all 91 no-win scenario card fines to be role-appropriate and relatable. Create tiered fine structures that match real-world accountability hierarchies, allowing 8-10 cards of gameplay per session while maintaining tension and educational value.

**Gap being closed:** Current fines are not role-appropriate (Vibe Coder facing $25M C-suite fines). Players experience only 1-2 cards before game over, making the game unplayable and fines unrelatable to actual role responsibilities.

**Purpose:** Make fines realistic to each role's decision-making authority. A developer should face "career-ending" fines ($100K-$8M), while C-suite faces "company-ending" fines ($5M-$500M).
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@data/cards/chief-something-officer.ts
@data/cards/head-of-something.ts
@data/cards/something-manager.ts
@data/cards/tech-ai-consultant.ts
@data/cards/data-scientist.ts
@data/cards/software-architect.ts
@data/cards/software-engineer.ts
@data/cards/vibe-coder.ts
@data/cards/vibe-engineer.ts
@data/cards/agentic-engineer.ts
@types.ts
@hooks/useGameState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define role-based fine tier constants</name>
  <files>types.ts, data/roles.ts (new file)</files>
  <action>
    Create fine tier structure in types.ts or a new data/roles.ts file:
    
    ```typescript
    export const ROLE_FINE_TIERS = {
      CHIEF_SOMETHING_OFFICER: { min: 5000000, max: 500000000, budget: 200000000 },
      HEAD_OF_SOMETHING: { min: 1000000, max: 50000000, budget: 100000000 },
      SOMETHING_MANAGER: { min: 500000, max: 25000000, budget: 75000000 },
      TECH_AI_CONSULTANT: { min: 300000, max: 15000000, budget: 60000000 },
      DATA_SCIENTIST: { min: 300000, max: 15000000, budget: 60000000 },
      SOFTWARE_ARCHITECT: { min: 500000, max: 20000000, budget: 75000000 },
      SOFTWARE_ENGINEER: { min: 200000, max: 10000000, budget: 50000000 },
      VIBE_CODER: { min: 100000, max: 8000000, budget: 40000000 },
      VIBE_ENGINEER: { min: 200000, max: 12000000, budget: 50000000 },
      AGENTIC_ENGINEER: { min: 300000, max: 18000000, budget: 60000000 }
    } as const;
    ```
    
    This establishes clear boundaries for what each role should face.
  </action>
  <verify>
    TypeScript compilation passes with new constants defined.
    Export is accessible from other modules.
  </verify>
  <done>
    ROLE_FINE_TIERS constant exists with 10 role entries, each with min/max fine and budget values.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebalance Chief Something Officer fines (C-suite tier)</name>
  <files>data/cards/chief-something-officer.ts</files>
  <action>
    Adjust all fines in chief-something-officer.ts to range $5M-$500M:
    - Current fines: $5M, $10M, $15M, $25M, $40M, $50M, $75M, $100M, $150M, $500M
    - These are already appropriate for C-suite! 
    - Verify distribution: ~20% at high end ($100M+), 50% mid ($25M-$75M), 30% low ($5M-$15M)
    - Check that "good" choices have $0 fines (no penalties for correct decisions)
    
    Only minor tweaks needed to ensure minimum is $5M (not lower) for any "bad" outcome.
  </action>
  <verify>
    grep 'fine:' data/cards/chief-something-officer.ts | grep -v 'fine: 0' | awk '{print $2}' | sort -n
    All non-zero fines >= 5000000 and <= 500000000
  </verify>
  <done>
    All Chief Something Officer fines are in $5M-$500M range. At least one $500M (catastrophic) fine exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rebalance Head of Something fines (management tier)</name>
  <files>data/cards/head-of-something.ts</files>
  <action>
    Scale Head of Something fines to $1M-$50M range:
    - Current: $500K, $1M, $2M, $3M, $4M, $5M, $6M, $8M, $15M
    - Move $500K up to $1M (minimum for management tier)
    - Keep $15M but ensure max doesn't exceed $50M
    - Add one or two higher fines ($25M-$50M) for major department failures
    - Ensure distribution: 30% low ($1M-$5M), 50% mid ($5M-$15M), 20% high ($15M-$50M)
  </action>
  <verify>
    grep 'fine:' data/cards/head-of-something.ts | grep -v 'fine: 0' | awk '{print $2}' | sort -n
    All non-zero fines >= 1000000 and <= 50000000
  </verify>
  <done>
    Head of Something fines scaled to $1M-$50M range with appropriate distribution.
  </done>
</task>

<task type="auto">
  <name>Task 4: Rebalance Something Manager fines</name>
  <files>data/cards/something-manager.ts</files>
  <action>
    Scale Something Manager fines to $500K-$25M range:
    - Current: $500K, $750K, $1M, $1.5M, $3M, $12M, $15M
    - Already mostly in range!
    - Ensure minimum bad fine is $500K (not lower)
    - Cap maximum at $25M (remove anything higher)
    - Distribution: 40% low ($500K-$3M), 40% mid ($3M-$10M), 20% high ($10M-$25M)
  </action>
  <verify>
    grep 'fine:' data/cards/something-manager.ts | grep -v 'fine: 0' | awk '{print $2}' | sort -n
    All non-zero fines >= 500000 and <= 25000000
  </verify>
  <done>
    Something Manager fines in $500K-$25M range.
  </done>
</task>

<task type="auto">
  <name>Task 5: Rebalance Tech/AI Consultant and Data Scientist fines</name>
  <files>data/cards/tech-ai-consultant.ts, data/cards/data-scientist.ts</files>
  <action>
    Scale both to $300K-$15M range (technical specialist tier):
    
    Tech/AI Consultant:
    - Current: $150K, $200K, $300K, $500K, $1M, $2.5M, $3M, $4M, $5M, $8M
    - Move $150K and $200K up to $300K minimum
    - Ensure max is $15M (currently $8M, need to add higher options)
    
    Data Scientist:
    - Current: $3M, $5M, $6M, $8M, $10M, $12M, $15M, $18M, $20M
    - Lower $18M and $20M to $15M max
    - Ensure minimum bad fine is $300K (not $3M)
    - Add some lower-tier fines ($300K-$1M range)
  </action>
  <verify>
    grep 'fine:' data/cards/tech-ai-consultant.ts | grep -v 'fine: 0' | awk '{print $2}' | sort -n
    grep 'fine:' data/cards/data-scientist.ts | grep -v 'fine: 0' | awk '{print $2}' | sort -n
    Both files: all fines >= 300000 and <= 15000000
  </verify>
  <done>
    Tech/AI Consultant and Data Scientist fines balanced to $300K-$15M range.
  </done>
</task>

<task type="auto">
  <name>Task 6: Rebalance Software Architect fines</name>
  <files>data/cards/software-architect.ts</files>
  <action>
    Scale Software Architect fines to $500K-$20M range:
    - Current: $500K, $1M, $2M, $5M, $8M, $10M, $12M, $15M, $20M, $25M
    - Lower $25M to $20M max
    - Ensure minimum bad fine is $500K
    - Distribution: 30% low ($500K-$2M), 40% mid ($2M-$10M), 30% high ($10M-$20M)
  </action>
  <verify>
    grep 'fine:' data/cards/software-architect.ts | grep -v 'fine: 0' | awk '{print $2}' | sort -n
    All fines >= 500000 and <= 20000000
  </verify>
  <done>
    Software Architect fines in $500K-$20M range.
  </done>
</task>

<task type="auto">
  <name>Task 7: Rebalance Software Engineer fines</name>
  <files>data/cards/software-engineer.ts</files>
  <action>
    Scale Software Engineer fines to $200K-$10M range:
    - Current: $1M, $3M, $4M, $5M, $6M, $15M, $20M
    - Lower $15M and $20M to $10M max
    - Add lower-tier fines ($200K-$500K range) for individual code mistakes
    - Distribution: 40% low ($200K-$2M), 40% mid ($2M-$6M), 20% high ($6M-$10M)
  </action>
  <verify>
    grep 'fine:' data/cards/software-engineer.ts | grep -v 'fine: 0' | awk '{print $2}' | sort -n
    All fines >= 200000 and <= 10000000
  </verify>
  <done>
    Software Engineer fines in $200K-$10M range with lower-tier options added.
  </done>
</task>

<task type="auto">
  <name>Task 8: Rebalance Vibe Coder fines (most junior)</name>
  <files>data/cards/vibe-coder.ts</files>
  <action>
    Scale Vibe Coder fines to $100K-$8M range (most junior/relatable):
    - Current: $500K, $1M, $5M, $8M, $10M, $12M, $15M, $18M, $20M, $25M
    - This is the MOST over-scaled role currently!
    - Lower $10M+ fines to $8M max
    - Add many lower-tier fines ($100K-$500K) for prompt mistakes, vibe-coding errors
    - Distribution: 50% low ($100K-$1M), 30% mid ($1M-$4M), 20% high ($4M-$8M)
    - Make this the most "forgiving" role for learning
  </action>
  <verify>
    grep 'fine:' data/cards/vibe-coder.ts | grep -v 'fine: 0' | awk '{print $2}' | sort -n
    All fines >= 100000 and <= 8000000
  </verify>
  <done>
    Vibe Coder fines scaled down to $100K-$8M range — most junior-friendly role.
  </done>
</task>

<task type="auto">
  <name>Task 9: Rebalance Vibe Engineer fines</name>
  <files>data/cards/vibe-engineer.ts</files>
  <action>
    Scale Vibe Engineer fines to $200K-$12M range:
    - Current: $1M, $2M, $3M, $4M, $5M, $8M, $10M, $12M, $15M, $25M
    - Lower $15M and $25M to $12M max
    - Add some lower-tier fines ($200K-$500K)
    - Distribution: 40% low ($200K-$2M), 40% mid ($2M-$7M), 20% high ($7M-$12M)
  </action>
  <verify>
    grep 'fine:' data/cards/vibe-engineer.ts | grep -v 'fine: 0' | awk '{print $2}' | sort -n
    All fines >= 200000 and <= 12000000
  </verify>
  <done>
    Vibe Engineer fines in $200K-$12M range.
  </done>
</task>

<task type="auto">
  <name>Task 10: Rebalance Agentic Engineer fines</name>
  <files>data/cards/agentic-engineer.ts</files>
  <action>
    Scale Agentic Engineer fines to $300K-$18M range:
    - Current: $500K, $1M, $2M, $3M, $5M, $8M, $10M, $12M, $15M, $20M, $25M, $30M, $35M, $40M
    - This is the HIGHEST current range! Way too high.
    - Lower $20M+ fines to $18M max
    - Add some lower-tier fines ($300K-$500K)
    - Distribution: 30% low ($300K-$2M), 40% mid ($2M-$8M), 30% high ($8M-$18M)
  </action>
  <verify>
    grep 'fine:' data/cards/agentic-engineer.ts | grep -v 'fine: 0' | awk '{print $2}' | sort -n
    All fines >= 300000 and <= 18000000
  </verify>
  <done>
    Agentic Engineer fines scaled from $40M max down to $18M max.
  </done>
</task>

<task type="auto">
  <name>Task 11: Implement role-based starting budgets</name>
  <files>hooks/useGameState.ts, types.ts</files>
  <action>
    Modify initialGameState or game initialization to use role-specific budgets:
    
    ```typescript
    // In useGameState.ts or game initialization
    const getInitialBudgetForRole = (role: ImpactZoneRole): number => {
      return ROLE_FINE_TIERS[role].budget;
    };
    
    // When creating new game state
    const createInitialState = (role: ImpactZoneRole): GameState => ({
      ...initialGameState,
      budget: getInitialBudgetForRole(role),
      role
    });
    ```
    
    Ensure budget is set correctly when player selects a role in role selection screen.
    Update any hardcoded $10M references to use dynamic lookup.
  </action>
  <verify>
    TypeScript compilation passes.
    Build succeeds.
    Game initializes with different budgets for different roles.
  </verify>
  <done>
    Role-specific starting budgets implemented: Vibe Coder starts with $40M, Chief Something Officer with $200M.
  </done>
</task>

<task type="auto">
  <name>Task 12: Update budget display in UI</name>
  <files>components/game/GameScreen.tsx (or budget display component)</files>
  <action>
    Ensure budget display shows the correct initial amount per role.
    Add visual indicator of starting budget vs current budget (optional).
    Test that budget updates correctly after fines are applied.
  </action>
  <verify>
    UI shows correct initial budget for each role.
    Budget decreases correctly when fines are applied.
  </verify>
  <done>
    Budget display correctly reflects role-specific starting amounts.
  </done>
</task>

<task type="auto">
  <name>Task 13: Run full test suite and validate balance</name>
  <files>tests/</files>
  <action>
    Run all tests to ensure no regressions:
    - npm run test:data (card validation tests)
    - npm run typecheck (TypeScript compilation)
    - npm run build (production build)
    
    Create a balance validation script that checks:
    - All fines are within role tier ranges
    - Starting budgets are correct per role
    - Average "bad" game can survive 8-10 cards
  </action>
  <verify>
    All tests pass.
    TypeScript compiles without errors.
    Build succeeds.
  </verify>
  <done>
    All tests passing, gameplay balanced for 8-10 cards per session.
  </done>
</task>

<task type="auto">
  <name>Task 14: Create SUMMARY.md and update tracking</name>
  <files>.planning/phases/03-no-win-scenario-cards/03-06-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Create comprehensive SUMMARY.md documenting:
    - Fine rebalancing approach and rationale
    - Role tier structure with min/max/budget values
    - Before/after comparison for each role
    - Gameplay impact (8-10 cards per session)
    
    Update STATE.md with session log entry.
    Update ROADMAP.md to mark Plan 03-06 complete.
  </action>
  <verify>
    SUMMARY.md exists with complete documentation.
    STATE.md updated.
    ROADMAP.md updated.
  </verify>
  <done>
    All documentation complete. Gap closure plan finished.
  </done>
</task>

</tasks>

<verification>
- [ ] All 91 cards have fines within role-appropriate ranges
- [ ] C-suite: $5M-$500M, Management: $1M-$50M, Technical: $300K-$25M, Individual: $100K-$20M
- [ ] Starting budgets scale: $40M (Vibe Coder) to $200M (Chief Something Officer)
- [ ] Players can survive 8-10 bad cards before game over
- [ ] TypeScript compilation passes
- [ ] Build succeeds with no errors
- [ ] All data validation tests pass
</verification>

<success_criteria>
- Each role has relatable, realistic fines for their decision-making level
- Gameplay allows 8-10 cards per session with meaningful choices
- C-suite feels the weight of massive fines; developers face manageable penalties
- All 10 roles have distinct fine profiles matching real-world accountability
- No card has fines outside its role tier
</success_criteria>

<output>
After completion, create `.planning/phases/03-no-win-scenario-cards/03-06-SUMMARY.md`
</output>