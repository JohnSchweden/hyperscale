---
phase: 04-immersive-pressure-effects
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - components/game/FeedbackOverlay.tsx
  - App.tsx
  - hooks/usePressureAudio.ts
autonomous: true
gap_closure: true
requirements:
  - IMMERSE-02
  - IMMERSE-03
  - IMMERSE-05

must_haves:
  truths:
    - "HUD escalation (Critical labels, color shifts) is visible in default Dev path"
    - "Haptic fires from user-gesture handlers when possible"
  artifacts:
    - path: "components/game/FeedbackOverlay.tsx"
      provides: "Pressure summary when budget/heat critical"
    - path: "App.tsx"
      provides: "vibrate called sync in swipe button handlers"
  key_links:
    - from: "App.tsx"
      to: "FeedbackOverlay"
      via: "budget, heat props when feedback shown"
    - from: "App.tsx onSwipeLeft/onSwipeRight"
      via: "triggerHaptic() sync before swipeProgrammatically"
---

<objective>
Make HUD escalation visible in the default path and move haptic to user-gesture handlers so it works on mobile.
</objective>

<context>
@.planning/phases/04-immersive-pressure-effects/04-UAT.md
@.planning/debug/hud-escalation-not-visible.md
@.planning/debug/haptic-not-on-mobile.md

Gap 3: Development deck + feedback overlay covers HUD; user never sees playing screen in escalated state.
Gap 6: navigator.vibrate from useEffect/setTimeout blocked by Chrome; must run from sync user-gesture (click, tap).
</context>

<interfaces>
From components/game/FeedbackOverlay.tsx:
- Props: personality, text, lesson, choice, fine, violation, teamImpact?, onNext
- Add: budget?: number; heat?: number (optional — when provided and critical, show escalation bar)

From App.tsx swipe handlers (lines 391-392):
- onSwipeLeft: () => swipe.swipeProgrammatically("LEFT")
- onSwipeRight: () => swipe.swipeProgrammatically("RIGHT")
- pressure.isCritical, pressure.isUrgent available in scope
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: HUD escalation visible in FeedbackOverlay</name>
  <files>components/game/FeedbackOverlay.tsx, App.tsx</files>
  <action>
Show escalation state in/alongside the feedback overlay when budget or heat is critical. This addresses the default path where overlay covers HUD.

1. Extend FeedbackOverlay props: budget?: number, heat?: number.
2. In FeedbackOverlay: when budget and/or heat provided, compute budgetCritical (budget &lt; 2_000_000), heatCritical (heat &gt;= 85), heatHigh (heat &gt;= 70). Use same thresholds as GameHUD (BUDGET_CRITICAL, HEAT_CRITICAL, HEAT_HIGH).
3. When budgetCritical or heatCritical: render a compact pressure banner above the modal content (inside the overlay). E.g. a narrow bar with "Budget Critical" or "Risk Critical" and the value. Reuse GameHUD styling (text-red-500, text-amber, etc.) or a minimal inline variant.
4. In App.tsx: when rendering FeedbackOverlay, pass budget={state.budget} heat={state.heat}.
</action>
  <verify>
    <automated>bunx playwright test tests/stage-snapshots.spec.ts tests/immersive-pressure-cues.spec.ts -x</automated>
  </verify>
  <done>When feedback overlay shows after a choice that put budget/heat critical, user sees escalation banner (Critical label, values).</done>
</task>

<task type="auto">
  <name>Task 2: Haptic from user-gesture handlers</name>
  <files>App.tsx, hooks/usePressureAudio.ts</files>
  <action>
Call navigator.vibrate only from synchronous handlers attached to user events. Chrome blocks vibrate when invoked from useEffect/setTimeout.

1. In App.tsx: change onSwipeLeft and onSwipeRight handlers. Before calling swipe.swipeProgrammatically(direction), synchronously call vibrate when (pressure.isCritical || pressure.isUrgent). Use the same pattern as existing: if navigator?.vibrate, navigator.vibrate([50, 30, 50]). This runs in the click handler — user gesture.

2. In hooks/usePressureAudio.ts: remove or gate the useEffect that calls triggerHaptic when isCritical. Either remove it entirely (rely on swipe-button path) or keep as fallback with a comment that it may not work on mobile. Per UAT: "accept timer-expiry haptics may not work on mobile" — handleTimerExpiry vibrate can remain; it will not work on Chrome mobile but does not break anything.

3. Ensure the swipe buttons (CardStack) are the primary path: they receive onSwipeLeft/onSwipeRight from App, which will now include the sync vibrate. The touch-swipe path (useSwipeGestures -> handleChoice) goes through rAF/setTimeout; keep that as-is — no vibrate there, accept it may not work for touch swipes.
</action>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Vibrate called synchronously from onClick of swipe buttons when critical/urgent. Timer-expiry haptic unchanged (may not work on mobile).</done>
</task>

</tasks>

<verification>
- Make choices that put heat/budget critical; feedback overlay shows escalation banner
- On mobile with vibration: tap swipe button when urgent/critical card — device vibrates
</verification>

<success_criteria>
Gaps 3 and 6 addressed. Escalation visible in overlay; haptic from gesture where possible.
</success_criteria>

<output>
After completion, create .planning/phases/04-immersive-pressure-effects/04-05-SUMMARY.md
</output>
