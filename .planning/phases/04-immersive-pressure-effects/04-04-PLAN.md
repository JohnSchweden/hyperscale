---
phase: 04-immersive-pressure-effects
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useCountdown.ts
  - services/pressureAudio.ts
autonomous: true
gap_closure: true
requirements:
  - IMMERSE-01
  - IMMERSE-02
  - IMMERSE-03

must_haves:
  truths:
    - "Urgent countdown is visible and counts down from startFrom"
    - "Timer expiry does not fire immediately on activation"
    - "Stress audio plays when pressure is high"
  artifacts:
    - path: "hooks/useCountdown.ts"
      provides: "Count reset on activation; no onComplete when count=0 and startFrom>0"
    - path: "services/pressureAudio.ts"
      provides: "Audio only after ctx.resume() completes"
  key_links:
    - from: "hooks/useCountdown.ts"
      to: "Pressure flow"
      via: "count reset when isActive goes false→true"
    - from: "services/pressureAudio.ts"
      via: "await ctx.resume() before oscillators"
---

<objective>
Fix useCountdown reset-on-activation and pressureAudio ctx.resume() so countdown, stress visuals, and stress audio work correctly.
</objective>

<context>
@.planning/phases/04-immersive-pressure-effects/04-UAT.md

Gaps 1, 2, 4 share root cause: useCountdown does not re-initialize count when isActive switches false→true. On activation count stays 0, onComplete runs immediately.
Gap 5 root cause: ctx.resume() not awaited; oscillators start while context suspended, audio never plays.
</context>

<tasks>

<task type="auto">
  <name>Task 1: useCountdown reset on activation</name>
  <files>hooks/useCountdown.ts</files>
  <action>
When isActive becomes true, set count to startFrom before starting the tick loop. When count===0 and startFrom>0, treat as fresh activation: call setCount(startFrom) and return early — do NOT call onComplete.

Current bug: Effect runs with isActive=true, count=0. It skips the count>0 branch and calls onComplete() immediately.

Fix: In the effect, when isActive is true:
- If count===0 and startFrom>0: setCount(startFrom), return (no timer, no onComplete). Next run will see count>0.
- If count>0: run existing tick logic.
- If count===0 and startFrom===0: call onComplete (legitimate expiry).

Preserve the inactive branch: when !isActive, setCount(startFrom) and return.
</action>
  <verify>
    <automated>bun run test -- tests/immersive-pressure-cues.spec.ts tests/stage-snapshots.spec.ts -x</automated>
  </verify>
  <done>Urgent card shows visible countdown; timer expiry happens only after count reaches 0; no instant feedback overlay on game start.</done>
</task>

<task type="auto">
  <name>Task 2: pressureAudio await ctx.resume()</name>
  <files>services/pressureAudio.ts</files>
  <action>
Make playPulse and startAlert resume the context before creating oscillators. ctx.resume() returns a Promise — await it before any oscillator creation or start.

For playPulse: add `if (ctx.state === "suspended") { await ctx.resume(); }` at the top, then create/start oscillators. Change playPulse to async.

For startAlert: same — await ctx.resume() when suspended before creating alertOsc.

For startHeartbeat: before the first playPulse() and setInterval, add `if (ctx.state === "suspended") await ctx.resume();`. Make startHeartbeat async. Callers (usePressureAudio) need not await; fire-and-forget is fine.

The PressureAudioSession interface can stay startHeartbeat(): void; startAlert(): void — callers ignore the promise. Implementation returns Promise&lt;void&gt;.
</action>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Stress audio (heartbeat/alert) plays when heat is high and user is on playing screen; no overlapping duplicate loops.</done>
</task>

</tasks>

<verification>
- Start game with Dev role, first card urgent: countdown visible, counts down
- Let countdown reach 0: feedback overlay appears (not instantly)
- Make choices that raise heat: heartbeat/alert audio plays
</verification>

<success_criteria>
Gaps 1, 2, 4, 5 addressed. useCountdown resets correctly; pressureAudio awaits resume.
</success_criteria>

<output>
After completion, create .planning/phases/04-immersive-pressure-effects/04-04-SUMMARY.md
</output>
