---
phase: "01-live-api-stt-research"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "src/services/geminiLive.ts"
  - "src/hooks/useLiveAPISpeechRecognition.ts"
  - "src/components/RoastTerminal.tsx"
autonomous: true
must_haves:
  truths:
    - "User can speak and see their speech transcribed in real-time"
    - "Transcription works with the existing mic button in RoastTerminal"
    - "Audio is correctly processed (48kHz → 16kHz PCM)"
  artifacts:
    - path: "src/services/geminiLive.ts"
      provides: "LiveConnectConfig with inputAudioTranscription support"
      exports: ["connect", "LiveConnectConfig"]
    - path: "src/hooks/useLiveAPISpeechRecognition.ts"
      provides: "Speech recognition hook with startRecording/stopRecording"
      exports: ["useLiveAPISpeechRecognition"]
    - path: "src/components/RoastTerminal.tsx"
      provides: "UI integration with mic button and transcript display"
  key_links:
    - from: "RoastTerminal.tsx"
      to: "useLiveAPISpeechRecognition"
      via: "hook call with onTranscript callback"
      pattern: "useLiveAPISpeechRecognition.*onTranscript"
---

<objective>
Implement speech-to-text (STT) for microphone input using Gemini Live API

Purpose: Allow users to speak their roast responses instead of typing
Output: Working voice input in RoastTerminal with real-time transcription
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-live-api-stt-research/01-RESEARCH.md

**Key implementation details from research:**
- Use `inputAudioTranscription: {}` config to enable STT
- Create AudioWorklet for 48kHz → 16kHz PCM conversion
- Stream audio via `session.sendRealtimeInput({ audio: { data, mimeType } })`
- Receive transcription via `serverContent.inputTranscription.text`
- Debounce transcription chunks (1.5s) for final result
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update geminiLive.ts with STT config</name>
  <files>src/services/geminiLive.ts</files>
  <action>
    Modify the existing geminiLive.ts service:
    1. Add `inputAudioTranscription` to LiveConnectConfig type (empty object enables it)
    2. Add callback handler for `inputTranscription` in onmessage
    3. Export the updated config type
    
    Reference the existing service structure - it already has connect() and LiveSession types.
    Use the research file for the exact config shape.
  </action>
  <verify>grep -q "inputAudioTranscription" src/services/geminiLive.ts && echo "Config updated"</verify>
  <done>geminiLive.ts has inputAudioTranscription config option and transcription callback</done>
</task>

<task type="auto">
  <name>Task 2: Create useLiveAPISpeechRecognition hook</name>
  <files>src/hooks/useLiveAPISpeechRecognition.ts</files>
  <action>
    Create new hook following research pattern:
    1. Implement startRecording() - creates session, starts mic capture
    2. Implement stopRecording() - sends audioStreamEnd, closes session
    3. Track transcript state (streaming + final)
    4. Handle isRecording boolean state
    
    Use inline AudioWorklet for PCM conversion (Float32 → Int16):
    - Create AudioContext at 16kHz sample rate
    - Use AudioWorkletProcessor to convert samples
    - Send base64-encoded chunks via session.sendRealtimeInput
    
    Return: { startRecording, stopRecording, transcript, isRecording, error }
  </action>
  <verify>ls src/hooks/useLiveAPISpeechRecognition.ts && grep -q "startRecording" src/hooks/useLiveAPISpeechRecognition.ts</verify>
  <done>Hook created with startRecording, stopRecording, transcript, isRecording exports</done>
</task>

<task type="auto">
  <name>Task 3: Integrate STT into RoastTerminal</name>
  <files>src/components/RoastTerminal.tsx</files>
  <action>
    Integrate the new hook into existing RoastTerminal:
    1. Import useLiveAPISpeechRecognition
    2. Wire existing mic button to startRecording/stopRecording
    3. Display streaming transcript in UI (near mic button)
    4. On stopRecording, populate the input field with final transcript
    
    Existing mic button already exists - just wire it to the new hook.
    Show "Listening..." state while recording, show transcript as it streams.
  </action>
  <verify>grep -q "useLiveAPISpeechRecognition" src/components/RoastTerminal.tsx && echo "Integration done"</verify>
  <done>Mic button triggers recording, transcript appears in UI, final transcript fills input</done>
</task>

</tasks>

<verification>
Run existing tests to ensure no regressions:
- bun run test -- tests/roast-terminal.spec.ts (or relevant test file)
- Manual verification: click mic, speak, see transcription
</verification>

<success_criteria>
- [ ] geminiLive.ts supports inputAudioTranscription config
- [ ] useLiveAPISpeechRecognition hook created with proper exports
- [ ] RoastTerminal mic button enables voice input
- [ ] User speech is transcribed in real-time
- [ ] Final transcript populates input field on stop
- [ ] Existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-live-api-stt-research/01-01-SUMMARY.md`
</output>
