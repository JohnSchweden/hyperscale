---
phase: 12-gameplay-tweaks-and-card-variety
plan: 02
type: TDD
wave: 2
depends_on:
  - "01"
files_modified:
  - data/cards/development.ts
  - data/cards/management.ts
  - data/cards/finance.ts
autonomous: true
must_haves:
  truths:
    - "Development gameplay includes new incidents from tools beyond the original IDE and terminal surfaces."
    - "Management gameplay includes new incidents from planning and meeting surfaces that fit leadership workflows."
    - "Finance gameplay includes new incidents from business-document and meeting surfaces so the deck feels less repetitive."
  artifacts:
    - path: "data/cards/development.ts"
      provides: "Additional development role cards using new sources"
      contains: "export const DEVELOPMENT_CARDS"
    - path: "data/cards/management.ts"
      provides: "Additional management role cards using new sources"
      contains: "export const MANAGEMENT_CARDS"
    - path: "data/cards/finance.ts"
      provides: "Additional finance role cards using new sources"
      contains: "export const FINANCE_CARDS"
  key_links:
    - from: "data/cards/development.ts"
      to: "types.ts"
      via: "AppSource enum references in new card definitions"
      pattern: "AppSource\\."
    - from: "data/cards/management.ts"
      to: "types.ts"
      via: "AppSource enum references in new card definitions"
      pattern: "AppSource\\."
    - from: "data/cards/finance.ts"
      to: "types.ts"
      via: "AppSource enum references in new card definitions"
      pattern: "AppSource\\."
---

<objective>
Add new role-specific cards for the engineering and business decks using the expanded source surfaces from Plan 01.

Purpose: Phase 12 needs broader scenario variety across roles, not just new enum values sitting uselessly in `types.ts` like decorative compliance theater.
Output: New cards in `development.ts`, `management.ts`, and `finance.ts` that use the new source values and fit each role's existing tone and risk profile.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@types.ts
@data/cards/development.ts
@data/cards/management.ts
@data/cards/finance.ts
@components/game/CardStack.tsx
</context>

<tdd_config>
This plan is `execute`, not `tdd`.
- The work is content expansion inside existing role decks.
- The main risk is fit, wiring, and gameplay rendering, which are better validated via build and E2E flow than isolated unit-first design.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Add new development incidents using the expanded sources</name>
  <files>data/cards/development.ts</files>
  <action>Add at least two new development cards that use newly introduced `AppSource` values such as `JIRA`, `NOTION`, or `MEETING`. Keep the cards aligned with the deck's existing shape: unique `id`, role-consistent sender/context copy, realistic AI governance failure modes, complete left/right outcomes, and full personality feedback.</action>
  <verify>Run `bun run build` and confirm the development deck compiles with the new card objects and enum references.</verify>
  <done>`DEVELOPMENT_CARDS` contains multiple new cards using new sources, with complete outcome data and no duplicate IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Add new management incidents using the expanded sources</name>
  <files>data/cards/management.ts</files>
  <action>Add at least two new management cards that use the new source surfaces in ways that feel natural for leadership workflows, such as strategy docs, planning systems, or meeting summaries. Match the existing deck's satirical tone and legal/compliance framing instead of drifting into generic office comedy.</action>
  <verify>Run `bun run build` and confirm the management deck compiles cleanly with the new source-backed cards.</verify>
  <done>`MANAGEMENT_CARDS` includes multiple new cards tied to the new app surfaces and preserves the deck's tone, structure, and type safety.</done>
</task>

<task type="auto">
  <name>Task 3: Add new finance incidents and verify the expanded decks in gameplay</name>
  <files>data/cards/finance.ts</files>
  <action>Add at least two new finance cards using new source surfaces, then run a targeted gameplay verification pass that ensures the shared card UI can render the added engineering, management, and finance incidents without breaking interaction flow. Update any snapshots only if the final rendered output changed intentionally.</action>
  <verify>Run `bun run build` followed by `bunx playwright test tests/stage-snapshots.spec.ts tests/swipe-interactions.spec.ts`.</verify>
  <done>`FINANCE_CARDS` has multiple new source-backed cards, the app builds, and gameplay tests pass with the expanded deck content.</done>
</task>

</tasks>

<verification>
Complete this plan only when:
- All three targeted decks have new cards using sources introduced by Plan 01.
- `bun run build` passes after the deck additions.
- Gameplay Playwright coverage still passes after the larger card pools are loaded.
</verification>

<success_criteria>
- The development, management, and finance roles each gain new scenario variety through the expanded source set.
- New cards are complete `Card` objects, not stubs or partial placeholders.
- The deck additions require no further shared-model changes beyond Plan 01.
</success_criteria>

<output>
After completion, create `.planning/phases/12-gameplay-tweaks-and-card-variety/12-02-SUMMARY.md`
</output>
