---
phase: 12-gameplay-tweaks-and-card-variety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types.ts
  - data/sources.ts
  - components/game/CardStack.tsx
autonomous: true
must_haves:
  truths:
    - "Gameplay cards can appear from more than the original four app surfaces."
    - "Players can recognize each card's source in the card header without confusing fallback styling."
    - "Existing cards still render correctly after the source expansion."
  artifacts:
    - path: "types.ts"
      provides: "Expanded AppSource enum for card metadata"
      contains: "export enum AppSource"
    - path: "data/sources.ts"
      provides: "SOURCE_ICONS Record mapping each AppSource to Font Awesome class"
      contains: "SOURCE_ICONS"
    - path: "components/game/CardStack.tsx"
      provides: "Source header rendering for current and next cards"
      contains: "currentCard.source"
  key_links:
    - from: "data/sources.ts"
      to: "components/game/CardStack.tsx"
      via: "SOURCE_ICONS[card.source] for header icon"
      pattern: "SOURCE_ICONS"
    - from: "types.ts"
      to: "components/game/CardStack.tsx"
      via: "AppSource enum import and source-specific UI mapping"
      pattern: "AppSource"
---

<objective>
Extend the shared card source model so new scenario surfaces can exist without hacks, and make the gameplay header render those sources intentionally.

Purpose: Phase 12 still needs scenario variety from expanded `AppSource` values; this plan lays the shared foundation that every new card deck addition depends on.
Output: Expanded source enum support in `types.ts` and updated source display logic in `components/game/CardStack.tsx`.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@types.ts
@components/game/CardStack.tsx
</context>

<tdd_config>
This plan is `execute`, not `tdd`.
- The work is shared type expansion plus UI glue for existing card metadata.
- Verification should rely on build/test execution and gameplay rendering checks rather than red-green API or pure function cycles.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Expand the shared card source enum</name>
  <files>types.ts</files>
  <action>Add additional `AppSource` enum values for the new cosmetic surfaces used by Phase 12 card content. Include the roadmap examples (`JIRA`, `NOTION`, `MEETING`) and keep naming aligned with existing all-caps enum style so card data can reference them directly without string literals.</action>
  <verify>Run `bun run build` and confirm TypeScript accepts the expanded enum without breaking existing card type usage.</verify>
  <done>`types.ts` exports the old sources plus the new Phase 12 sources, and card files can import them without type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add SOURCE_ICONS and replace source header fallback logic</name>
  <files>data/sources.ts, components/game/CardStack.tsx</files>
  <action>Create `data/sources.ts` with `SOURCE_ICONS: Record&lt;AppSource, string&gt;` mapping each enum value to a Font Awesome solid class (e.g. SLACK→fa-hashtag, EMAIL→fa-envelope, TERMINAL→fa-terminal, IDE→fa-terminal, JIRA→fa-list-check, NOTION→fa-file-lines, MEETING→fa-users). Refactor CardStack so both current and next card headers use `SOURCE_ICONS[card.source] ?? "fa-hashtag"` instead of the IDE-vs-else ternary. Preserve existing visual structure.</action>
  <verify>Run `bun run build` and inspect the card header rendering in gameplay with `bunx playwright test tests/stage-snapshots.spec.ts tests/swipe-interactions.spec.ts`.</verify>
  <done>`CardStack.tsx` contains explicit source rendering logic that supports existing and new app surfaces for both visible card layers.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the shared foundation supports old and new deck content</name>
  <files>types.ts, data/sources.ts, components/game/CardStack.tsx</files>
  <action>Run the smallest meaningful verification pass for the shared foundation: build the app and execute the gameplay-focused Playwright coverage that exercises card rendering and swipe flow. If snapshot text or source headers need updates because of intentional source label changes, update them within this plan.</action>
  <verify>Run `bun run build` followed by `bunx playwright test tests/stage-snapshots.spec.ts tests/swipe-interactions.spec.ts`.</verify>
  <done>The app builds, gameplay tests pass, and card headers remain legible with no regressions from the source-model change.</done>
</task>

</tasks>

<verification>
Complete this plan only when:
- `bun run build` passes.
- Gameplay-focused Playwright coverage passes for rendering and interaction.
- The card header can present any source introduced in Phase 12 without relying on an accidental fallback branch.
</verification>

<success_criteria>
- `AppSource` supports additional scenario surfaces needed by Phase 12.
- Card header rendering is explicit and maintainable for both current and preview cards.
- This plan leaves downstream card-content plans free to add cards without touching shared source UI again.
</success_criteria>

<output>
After completion, create `.planning/phases/12-gameplay-tweaks-and-card-variety/12-01-SUMMARY.md`
</output>
