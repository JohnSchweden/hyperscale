---
phase: 12-gameplay-tweaks-and-card-variety
plan: 03
type: TDD
wave: 2
depends_on:
  - "01"
files_modified:
  - data/cards/marketing.ts
  - data/cards/hr.ts
  - data/cards/cleaning.ts
autonomous: true
must_haves:
  truths:
    - "Marketing gameplay includes incidents from more varied work surfaces than just Slack and email."
    - "HR gameplay includes incidents sourced from planning docs or meetings that fit people-process workflows."
    - "The cleaning deck no longer feels like a one-card afterthought once Phase 12 card variety is complete."
  artifacts:
    - path: "data/cards/marketing.ts"
      provides: "Additional marketing role cards using new sources"
      contains: "export const MARKETING_CARDS"
    - path: "data/cards/hr.ts"
      provides: "Additional HR role cards using new sources"
      contains: "export const HR_CARDS"
    - path: "data/cards/cleaning.ts"
      provides: "Additional cleaning role cards using new sources"
      contains: "export const CLEANING_CARDS"
  key_links:
    - from: "data/cards/marketing.ts"
      to: "types.ts"
      via: "AppSource enum references in new card definitions"
      pattern: "AppSource\\."
    - from: "data/cards/hr.ts"
      to: "types.ts"
      via: "AppSource enum references in new card definitions"
      pattern: "AppSource\\."
    - from: "data/cards/cleaning.ts"
      to: "types.ts"
      via: "AppSource enum references in new card definitions"
      pattern: "AppSource\\."
---

<objective>
Add new role-specific cards for the comms, people, and facilities decks using the expanded Phase 12 source surfaces.

Purpose: Phase 12 is not complete until the new sources actually show up across roles, including the decks that currently have the thinnest variety.
Output: New cards in `marketing.ts`, `hr.ts`, and `cleaning.ts` that use the expanded app sources and preserve each deck's narrative voice.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@types.ts
@data/cards/marketing.ts
@data/cards/hr.ts
@data/cards/cleaning.ts
@components/game/CardStack.tsx
</context>

<tdd_config>
This plan is `execute`, not `tdd`.
- The work is deck-content expansion across existing files.
- Success depends on complete card objects, role fit, and end-to-end rendering rather than a pure input/output contract.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Add new marketing incidents using the expanded sources</name>
  <files>data/cards/marketing.ts</files>
  <action>Add at least two new marketing cards that use new `AppSource` values such as `NOTION`, `JIRA`, or `MEETING` where they fit campaign approvals, content planning, or agency coordination. Keep the copy sharp, role-appropriate, and structured exactly like the existing cards with complete outcomes and personality feedback.</action>
  <verify>Run `bun run build` and confirm the marketing deck compiles with the new source-backed card definitions.</verify>
  <done>`MARKETING_CARDS` contains multiple new cards using Phase 12 sources, all with complete `Card` shape and unique IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Add new HR incidents using the expanded sources</name>
  <files>data/cards/hr.ts</files>
  <action>Add at least two new HR cards using the new app surfaces in contexts like interview notes, performance review docs, or hiring meetings. Preserve the existing governance/compliance framing so the cards feel like real people-risk scenarios rather than filler.</action>
  <verify>Run `bun run build` and confirm the HR deck compiles cleanly with the added cards and source enum references.</verify>
  <done>`HR_CARDS` includes multiple new source-backed scenarios with complete outcomes, lessons, and personality feedback.</done>
</task>

<task type="auto">
  <name>Task 3: Expand the cleaning deck and verify the people/comms/ops slice</name>
  <files>data/cards/cleaning.ts</files>
  <action>Add enough new cleaning-role cards to move the deck beyond its current single-card state, using newly supported sources only where they feel plausible for facilities and incidental exposure scenarios. After the additions, run targeted gameplay verification for the affected decks and update snapshots only if the final UI output changed intentionally.</action>
  <verify>Run `bun run build` followed by `bunx playwright test tests/stage-snapshots.spec.ts tests/swipe-interactions.spec.ts`.</verify>
  <done>`CLEANING_CARDS` is no longer a one-card deck, the new cards use supported sources sensibly, and gameplay tests pass with the expanded content.</done>
</task>

</tasks>

<verification>
Complete this plan only when:
- Marketing, HR, and cleaning all gain new cards using the expanded source set.
- The cleaning deck is materially expanded, not tokenistically padded with one lazy duplicate scenario.
- Build and gameplay-focused Playwright verification pass after the additions.
</verification>

<success_criteria>
- The marketing and HR roles now present more varied scenario surfaces.
- The cleaning role is no longer the neglected intern of the deck system.
- Phase 12 source variety reaches all current role groups without overlapping files with Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/12-gameplay-tweaks-and-card-variety/12-03-SUMMARY.md`
</output>
