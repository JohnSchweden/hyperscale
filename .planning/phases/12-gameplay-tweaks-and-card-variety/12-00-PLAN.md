---
phase: 12-gameplay-tweaks-and-card-variety
plan: 00
type: TDD
wave: 1
depends_on: []
files_modified:
  - lib/deck.ts
  - types.ts
  - hooks/useGameState.ts
  - App.tsx
  - components/game/CardStack.tsx
  - components/game/GameScreen.tsx
  - data/cards/index.ts
autonomous: true
must_haves:
  truths:
    - "Card order varies between game runs (shuffled on start)."
    - "Some cards appear only after specific prior choices (branching)."
    - "Consumers use the resolved deck, not ROLE_CARDS directly."
  artifacts:
    - path: "lib/deck.ts"
      provides: "shuffleDeck and resolveDeckWithBranching"
      exports: ["shuffleDeck", "resolveDeckWithBranching"]
    - path: "data/cards/index.ts"
      provides: "BRANCH_INJECTIONS map for conditional card injection"
      contains: "BRANCH_INJECTIONS"
    - path: "hooks/useGameState.ts"
      provides: "effectiveDeck in state, set on PLAYING transition"
      contains: "effectiveDeck"
  key_links:
    - from: "App.tsx"
      to: "lib/deck.ts"
      via: "compute deck on INITIALIZING→PLAYING, dispatch with shuffledDeck"
      pattern: "shuffleDeck|resolveDeckWithBranching"
    - from: "hooks/useGameState.ts"
      to: "lib/deck.ts"
      via: "reducer NEXT_INCIDENT uses effectiveDeck, injection on advance"
      pattern: "effectiveDeck"
---

<objective>
Implement TWEAK-01 (shuffle deck on game start) and TWEAK-02 (branching card logic) so card order varies and some cards only appear after prior choices.

Purpose: ROADMAP marks these done but they are not implemented. Research confirms no shuffle on INITIALIZING→PLAYING and no conditional card injection.
Output: lib/deck.ts with shuffle and branching utilities, GameState.effectiveDeck, wired consumers.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-gameplay-tweaks-and-card-variety/12-RESEARCH.md
@hooks/useGameState.ts
@hooks/useBossFight.ts
@App.tsx
@components/game/CardStack.tsx
</context>

<tdd_config>
TDD for testable logic: shuffleDeck (pure, no mutation) and resolveDeckWithBranching (conditional inclusion). Use execute for wiring.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Shuffle deck (TDD) — extract and wire</name>
  <files>lib/deck.ts, lib/deck.test.ts</files>
  <action>
    Extract Fisher-Yates from useBossFight._shuffleArray into lib/deck.ts as `shuffleDeck(cards: Card[]): Card[]`. Do not mutate input; return new array.
    TDD: Write test that (a) output has same length and elements as input, (b) input array is unchanged, (c) repeated calls produce different order (non-deterministic but test permutation property).
    Then wire: on INITIALIZING→PLAYING transition in App.tsx, compute `shuffled = shuffleDeck([...ROLE_CARDS[role]])` and dispatch STAGE_CHANGE with new payload `shuffledDeck`. Extend GameState with `effectiveDeck: Card[] | null`, GameAction STAGE_CHANGE with `shuffledDeck?: Card[]`, reducer to store it. All consumers (App, CardStack, useGameState NEXT_INCIDENT) use `state.effectiveDeck ?? ROLE_CARDS[role]` for the deck.
  </action>
  <verify>Run `bun test lib/deck.test.ts` then `bun run build` and `bunx playwright test`. Verify two game runs show different card order (manual or add deterministic-seed test).</verify>
  <done>Deck is shuffled on game start; effectiveDeck in state; no direct ROLE_CARDS use during PLAYING.</done>
</task>

<task type="auto">
  <name>Task 2: Branching logic (TDD) — conditional injection</name>
  <files>lib/deck.ts, data/cards/index.ts</files>
  <action>
    Add `BRANCH_INJECTIONS: Record&lt;string, Card[]&gt;` (key = `${cardId}:${choice}`) in data/cards/index.ts. When a player swipes on cardId with choice, inject the mapped cards after that position.
    Implement `resolveDeckWithBranching(effectiveDeck: Card[], history, currentCardIndex): Card[]` — use history[history.length-1] for lastCardId and choice; if BRANCH_INJECTIONS[`${lastCardId}:${choice}`] exists, splice those cards at currentCardIndex+1. Return new array.
    TDD: Test that (a) no history → no injection, (b) matching history entry → branch cards appear at correct position.
    Wire: In reducer NEXT_INCIDENT, before advancing index, call resolveDeckWithBranching with history and the choice from CHOICE_MADE (reducer receives it via a new action or we pass it — CHOICE_MADE precedes NEXT_INCIDENT, so we have history). Actually: NEXT_INCIDENT runs after feedback dismissal; at that point history already has the choice. Use history[history.length-1] to get lastCardId and choice. Compute new effectiveDeck with injection, then advance.
  </action>
  <verify>Run `bun test lib/deck.test.ts` then `bun run build` and `bunx playwright test`. Add one branch card to BRANCH_INJECTIONS and verify it appears after the matching choice.</verify>
  <done>Branch cards inject when prior choice matches BRANCH_INJECTIONS key; one example branch card proves it.</done>
</task>

<task type="auto">
  <name>Task 3: Integration and verification</name>
  <files>App.tsx, components/game/GameScreen.tsx, data/cards/development.ts</files>
  <action>
    Ensure GameScreen passes effectiveDeck (or state) to CardStack; CardStack receives cards prop instead of reading ROLE_CARDS[role]. Add one concrete branch card in development.ts and register it in BRANCH_INJECTIONS for an existing card's RIGHT swipe to inject a "consequence" card. Run full Playwright suite and fix any snapshot or flow regressions.
  </action>
  <verify>Run `bun run build` and `bunx playwright test`. Manual: play Development, swipe right on trigger card, confirm branch card appears.</verify>
  <done>Shuffle and branching both work; tests pass; no regressions.</done>
</task>

</tasks>

<verification>
- lib/deck.ts exists with shuffleDeck and resolveDeckWithBranching
- GameState has effectiveDeck; INITIALIZING→PLAYING sets it
- NEXT_INCIDENT applies branching injection when history matches
- CardStack and App use effectiveDeck, not ROLE_CARDS directly
- At least one branch card demonstrates the mechanic
</verification>

<success_criteria>
- TWEAK-01: Shuffle on game start — card order varies between runs
- TWEAK-02: Branching — some cards only appear after specific prior choices
- No mutation of ROLE_CARDS or source deck data
</success_criteria>

<output>
After completion, create `.planning/phases/12-gameplay-tweaks-and-card-variety/12-00-SUMMARY.md`
</output>
