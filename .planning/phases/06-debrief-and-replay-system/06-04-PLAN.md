---
phase: 06-debrief-and-replay-system
plan: 04
type: auto
wave: 2
depends_on: [06-02]
files_modified:
  - types.ts
  - hooks/useEmailCapture.ts
  - hooks/index.ts
  - components/game/debrief/DebriefPage3Verdict.tsx
  - components/game/debrief/EmailCaptureForm.tsx
  - api/v2-waitlist.ts
autonomous: true
requirements:
  - DEBRIEF-11

must_haves:
  truths:
    - "Email form validates basic email format before submission"
    - "Form submission POST to /api/v2-waitlist with email, role, archetype, resilience, timestamp"
    - "On successful submission (2xx response), form shows success message and disables input"
    - "Form handles network errors gracefully and shows error message"
    - "Email field is read-only after successful submission to prevent re-submission"
  artifacts:
    - path: "types.ts"
      provides: "V2WaitlistPayload interface"
      contains: "interface V2WaitlistPayload"
    - path: "hooks/useEmailCapture.ts"
      provides: "useEmailCapture hook managing form state, validation, submission"
      exports: ["useEmailCapture"]
    - path: "components/game/debrief/EmailCaptureForm.tsx"
      provides: "Email form component with validation and success/error states"
      exports: ["EmailCaptureForm"]
    - path: "api/v2-waitlist.ts"
      provides: "Backend API endpoint for V2 waitlist signup"
      exports: ["default"]
  key_links:
    - from: "components/game/debrief/EmailCaptureForm.tsx"
      to: "hooks/useEmailCapture.ts"
      via: "form state hook invocation"
      pattern: "useEmailCapture"
    - from: "hooks/useEmailCapture.ts"
      to: "api/v2-waitlist.ts"
      via: "fetch POST to /api/v2-waitlist"
      pattern: "fetch.*v2-waitlist"

---

<objective>
Implement email capture form for V2 waitlist on Page 3. Create form component with validation, submission handler, success/error feedback, and backend endpoint to persist signups.

Purpose: Capture lead emails from players to build V2 waitlist. Form is low-friction and appears only at the end of the debrief sequence to maximize conversion.

Output: Email capture hook, form component with validation and states, and backend API route for waitlist persistence.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-debrief-and-replay-system/06-ARCHITECTURE.md
@.planning/phases/06-debrief-and-replay-system/06-02-SUMMARY.md
@types.ts
@hooks/index.ts
@components/game/debrief/DebriefPage3Verdict.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define V2 waitlist types and create useEmailCapture hook</name>
  <files>types.ts, hooks/useEmailCapture.ts, hooks/index.ts</files>
  <behavior>
    - V2WaitlistPayload interface: {email, role, archetype, resilience, timestamp}
    - useEmailCapture hook returns: {email, setEmail, isSubmitting, error, success, submit}
    - submit() validates email format (basic: includes @ and .), then POSTs to /api/v2-waitlist
    - On success (2xx), sets success=true, clears email, persists in localStorage
    - On error, stores error message and shows to user
    - Memoized to prevent unnecessary re-creation
  </behavior>
  <action>Extend types.ts with V2WaitlistPayload interface (email, role, archetype, resilience, timestamp). Create hooks/useEmailCapture.ts with useEmailCapture hook. Hook manages: email string state, isSubmitting bool, error message, success bool. Implement submit() function that: (1) validates email format (regex: /^[^\s@]+@[^\s@]+\.[^\s@]+$/), (2) POSTs to /api/v2-waitlist with full payload, (3) on response.ok sets success=true, (4) on error stores error message. Export from hooks/index.ts. Write unit tests for validation logic and state management.</action>
  <verify>
    <automated>bun run test -- unit/email-capture.test.ts</automated>
  </verify>
  <done>types.ts includes V2WaitlistPayload. hooks/useEmailCapture.ts exports hook with email validation and submission. Unit tests pass: email validation correct, submission state management works, success/error states trigger correctly. No type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create EmailCaptureForm component</name>
  <files>components/game/debrief/EmailCaptureForm.tsx</files>
  <action>Create a form component that: (1) Renders input field for email (type="email", placeholder="your.email@company.com"). (2) Shows [Join V2 Waitlist] button below input (uses established button styling: white bg, black text, cyan hover). (3) Button disabled until valid email entered. (4) While isSubmitting, button shows "Joining..." and is disabled. (5) On success, shows success message: "✓ Email received. V2 is coming." in green box, input becomes read-only. (6) On error, shows error message in red. (7) Component accepts role, archetype, resilience as props to pass to submit(). Include HTML5 email validation as fallback.</action>
  <verify>
    <automated>bun run test -- tests/debrief-email-capture.spec.ts --grep "@smoke"</automated>
  </verify>
  <done>EmailCaptureForm.tsx renders email input, [Join V2 Waitlist] button, and success/error messages. Form validation and state transitions work. Button styling matches design system. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create backend API endpoint for V2 waitlist</name>
  <files>api/v2-waitlist.ts</files>
  <action>Create an API route handler (POST /api/v2-waitlist) that: (1) Accepts V2WaitlistPayload in request body. (2) Validates email format on server side (never trust client). (3) Logs signup to console or file (production: store in database). (4) Returns 200 OK with {success: true} on valid submission. (5) Returns 400 Bad Request on invalid email. (6) Catches errors and returns 500 with error message. For MVP, logging to console is sufficient; persistence can be added later. Include timestamp in log for tracking signup time.</action>
  <verify>
    <automated>bun run test -- unit/v2-waitlist-api.test.ts</automated>
  </verify>
  <done>api/v2-waitlist.ts exports POST handler. Handler validates email, logs signup, returns appropriate status codes. Unit tests pass: valid payload returns 200, invalid email returns 400, errors handled gracefully. No type errors.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate EmailCaptureForm into DebriefPage3Verdict</name>
  <files>components/game/debrief/DebriefPage3Verdict.tsx</files>
  <action>Update DebriefPage3Verdict to include EmailCaptureForm in the V2 upsell section (currently stubbed). Render form below text "This was the static test. The real Kobayashi Maru adapts to your weaknesses..." and "Enter your email to get early access to V2...". Pass role, archetype, resilience to form. Wrap form in a div with subtle border or background to visually distinguish it from buttons above.</action>
  <verify>
    <automated>bun run test -- tests/debrief-page-3.spec.ts --grep "email"</automated>
  </verify>
  <done>DebriefPage3Verdict.tsx includes EmailCaptureForm. Form is positioned below V2 upsell copy. Props correctly wired. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 5: Write email capture integration tests</name>
  <files>tests/debrief-email-capture.spec.ts, unit/email-capture.test.ts</files>
  <action>Add unit tests in unit/email-capture.test.ts: (1) Email validation rejects invalid formats. (2) useEmailCapture hook manages form state correctly. (3) Submit function POSTs to correct endpoint. Add E2E test in tests/debrief-email-capture.spec.ts: (1) Render form on Page 3. (2) Enter valid email. (3) Click [Join V2 Waitlist]. (4) Verify success message appears. (5) Verify email field becomes read-only. (6) Test invalid email shows validation error.</action>
  <verify>
    <automated>bun run test -- unit/email-capture.test.ts && bun run test -- tests/debrief-email-capture.spec.ts</automated>
  </verify>
  <done>All unit and E2E tests pass. Email validation works, form submission succeeds, success state shows, error handling works. Ready for deployment.</done>
</task>

</tasks>

<verification>
After Wave 2 tasks complete:
1. Run `bun run test -- unit/email-capture.test.ts` — all tests green
2. Run `bun run test -- tests/debrief-email-capture.spec.ts` — all tests green
3. Run `bun run test -- unit/v2-waitlist-api.test.ts` — all tests green
4. Verify email form appears on Page 3
5. Verify form submission POSTs to /api/v2-waitlist
6. Verify success message displays after valid submission
7. Check API logs for sample signup entry
</verification>

<success_criteria>
- Email capture hook created and tested
- EmailCaptureForm component with validation and states
- Backend API endpoint for /api/v2-waitlist created
- Form integrated into DebriefPage3Verdict
- Email validation (client and server) working correctly
- Success/error states displaying properly
- All tests passing
- DEBRIEF-11 requirement satisfied
- Ready for Wave 3: "Unlock all endings" gamification and reflection prompt
</success_criteria>

<output>
After completion, create `.planning/phases/06-debrief-and-replay-system/06-04-SUMMARY.md`
</output>
