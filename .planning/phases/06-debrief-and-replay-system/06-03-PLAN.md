---
phase: 06-debrief-and-replay-system
plan: 03
type: auto
wave: 2
depends_on: [06-02]
files_modified:
  - components/game/debrief/DebriefPage3Verdict.tsx
  - utils/linkedin-share.ts
  - lib/social-share.ts
autonomous: true
requirements:
  - DEBRIEF-10

must_haves:
  truths:
    - "LinkedIn share button opens LinkedIn share dialog with pre-filled text"
    - "Share text includes role title, archetype name, and resilience score"
    - "URL encoding is correct and share text is truncation-safe"
    - "Button opens in a new window without blocking main app"
  artifacts:
    - path: "utils/linkedin-share.ts"
      provides: "encodeLinkedInShareUrl(), formatShareText() helper functions"
      exports: ["encodeLinkedInShareUrl", "formatShareText"]
    - path: "components/game/debrief/DebriefPage3Verdict.tsx"
      provides: "[Share to LinkedIn] button with correct URL encoding"
      modified: "Implements LinkedIn share button functionality"
  key_links:
    - from: "components/game/debrief/DebriefPage3Verdict.tsx"
      to: "utils/linkedin-share.ts"
      via: "import and call encodeLinkedInShareUrl"
      pattern: "encodeLinkedInShareUrl"

---

<objective>
Implement LinkedIn share functionality for Page 3 (Verdict). Generate shareable text with role, archetype, and resilience score, encode it correctly for LinkedIn's native share endpoint, and ensure it opens in a new window.

Purpose: Enable viral growth loop: players share their archetype verdict on LinkedIn, bringing new users to the game.

Output: LinkedIn URL encoding utility, shareable text formatter, and [Share to LinkedIn] button integration on Page 3.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-debrief-and-replay-system/06-ARCHITECTURE.md
@.planning/phases/06-debrief-and-replay-system/06-02-SUMMARY.md
@types.ts
@data/roles.ts
@components/game/debrief/DebriefPage3Verdict.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LinkedIn share utility module</name>
  <files>utils/linkedin-share.ts</files>
  <behavior>
    - formatShareText(role: RoleType, archetypeName: string, resilience: number) → string
      Returns: "I just faced the Kobayashi Maru as a [ROLE_TITLE]. My Resilience Score: [XX]% ([ARCHETYPE])."
      Example: "I just faced the Kobayashi Maru as a CTO. My Resilience Score: 88% (Pragmatist)."
    - encodeLinkedInShareUrl(shareText: string) → string
      Returns: https://www.linkedin.com/sharing/share-offsite/?url=[ENCODED_CURRENT_URL]
      Uses encodeURIComponent for URL, does NOT encode share text in URL (LinkedIn handles via og:description)
    - getShareUrl(role: RoleType, archetype: Archetype, resilience: number) → string
      Combines formatShareText + encodeLinkedInShareUrl
  </behavior>
  <action>Create utils/linkedin-share.ts with: (1) formatShareText() function that takes role (RoleType), archetype name (string), and resilience (0-100) and returns formatted share text. Use ROLE_DESCRIPTIONS to get role title. Archetype name should be short (e.g., "Pragmatist" not "The Pragmatist: A leader..."). (2) encodeLinkedInShareUrl() that takes the current window URL, encodes it with encodeURIComponent, and returns the LinkedIn share endpoint. (3) getShareUrl() convenience function that combines both. Include unit tests verifying: text format is correct, URL encoding is safe, no truncation of key elements.</action>
  <verify>
    <automated>bun run test -- unit/linkedin-share.test.ts</automated>
  </verify>
  <done>utils/linkedin-share.ts exports formatShareText(), encodeLinkedInShareUrl(), getShareUrl(). Unit tests pass: text format correct, URL encoding safe, no truncation. No type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement [Share to LinkedIn] button on DebriefPage3Verdict</name>
  <files>components/game/debrief/DebriefPage3Verdict.tsx</files>
  <action>Update DebriefPage3Verdict to include a [Share to LinkedIn] button (primary CTA below [Reboot System]). Button onClick handler: call getShareUrl() with role, archetype, resilience, then open in new window using window.open(linkedinShareUrl, "_blank", "width=550,height=420"). Use established button styling (white background, black text, cyan hover). Ensure button is disabled if archetype is null (guard against invalid state).</action>
  <verify>
    <automated>bun run test -- tests/debrief-page-3.spec.ts --grep "linkedin"</automated>
  </verify>
  <done>DebriefPage3Verdict.tsx includes [Share to LinkedIn] button. onClick calls getShareUrl() and opens LinkedIn share dialog. Button styling matches design system. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Write LinkedIn share integration tests</name>
  <files>tests/debrief-page-3.spec.ts, unit/linkedin-share.test.ts</files>
  <action>Add unit tests in unit/linkedin-share.test.ts: (1) formatShareText produces correct format with role title + archetype + score. (2) Share text length is < 200 chars (optimal for LinkedIn preview). (3) encodeLinkedInShareUrl produces valid LinkedIn share URL. Add E2E test in tests/debrief-page-3.spec.ts: (1) Click [Share to LinkedIn] button. (2) Verify LinkedIn share dialog opens (check window.open was called). (3) Verify URL contains encoded current location.</action>
  <verify>
    <automated>bun run test -- unit/linkedin-share.test.ts && bun run test -- tests/debrief-page-3.spec.ts --grep "linkedin"</automated>
  </verify>
  <done>Unit and E2E tests pass. Share text format correct, URL encoding safe, button opens dialog correctly.</done>
</task>

</tasks>

<verification>
After Wave 2 tasks complete:
1. Run `bun run test -- unit/linkedin-share.test.ts` — all tests green
2. Run `bun run test -- tests/debrief-page-3.spec.ts --grep "linkedin"` — all tests green
3. Verify window.open is called with correct LinkedIn share URL on button click
4. Verify share text includes role, archetype, and resilience score
</verification>

<success_criteria>
- LinkedIn share utility created and tested
- [Share to LinkedIn] button implemented on Page 3
- Share text formatted correctly with role + archetype + score
- URL encoding safe and correct
- Button opens LinkedIn share dialog in new window
- E2E tests passing
- DEBRIEF-10 requirement satisfied
- Ready for Wave 3: email capture and V2 waitlist signup
</success_criteria>

<output>
After completion, create `.planning/phases/06-debrief-and-replay-system/06-03-SUMMARY.md`
</output>
