---
phase: 06-debrief-and-replay-system
plan: 12
type: auto
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - utils/linkedin-share.ts
autonomous: true

must_haves:
  truths:
    - "LinkedIn share URL includes formatted share text with role, archetype, and resilience score"
    - "Clicking LinkedIn button opens share dialog with pre-filled content"
  artifacts:
    - path: "utils/linkedin-share.ts"
      provides: "Proper share URL with summary parameter"
  key_links:
    - from: "formatShareText"
      to: "getShareUrl"
      via: "Call formatShareText and include result in URL"

issue_reference:
  test: 3
  issue: "LinkedIn button clickable but share dialog doesn't open"
---

<objective>
Fix LinkedIn share button: Ensure share text is included in the URL and dialog opens correctly.

Root cause: getShareUrl() never calls formatShareText() and doesn't include the text in the LinkedIn URL.

Fix: Update getShareUrl to call formatShareText and include the result as a summary parameter in the LinkedIn share URL.
</objective>

<files>
- utils/linkedin-share.ts
</files>

<implementation_notes>
Current issue in getShareUrl:
```typescript
export function getShareUrl(role, archetype, resilience, currentUrl?): string {
  const url = currentUrl ?? (typeof window !== "undefined" ? window.location.href : "");
  return encodeLinkedInShareUrl(url);  // Missing text parameter!
}
```

Fix needed:
1. Call formatShareText(role, archetype.name, resilience) to generate share text
2. Include the text as a summary parameter in the LinkedIn URL
3. LinkedIn supports: https://www.linkedin.com/sharing/share-offsite/?url={url}&summary={text}
</implementation_notes>

<verify>
1. Navigate to debrief Page 3
2. Click LinkedIn share button
3. Verify LinkedIn share dialog opens
4. Verify pre-filled text includes role, archetype, and resilience score
</verify>
