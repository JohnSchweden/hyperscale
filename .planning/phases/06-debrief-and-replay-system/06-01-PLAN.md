---
phase: 06-debrief-and-replay-system
plan: 01
type: auto
wave: 0
depends_on: []
files_modified:
  - types.ts
  - data/archetypes.ts
  - data/index.ts
  - hooks/useArchetype.ts
  - hooks/index.ts
autonomous: true
requirements:
  - DEBRIEF-01
  - DEBRIEF-02
  - DEBRIEF-12

must_haves:
  truths:
    - "Decision history (which cards, which choices) is preserved and accessible after game over"
    - "Archetype system maps decision patterns to personality types using weighted decision vectors"
    - "Resilience Score (0-100) is calculated as a single number based on decision consistency"
    - "Archetype calculation happens once when entering debrief, not repeatedly on page renders"
  artifacts:
    - path: "types.ts"
      provides: "ArchetypeId enum, Archetype interface, DebriefState interface"
      min_lines: 10
    - path: "data/archetypes.ts"
      provides: "ARCHETYPES mapping, calculateArchetype(), calculateResilienceScore()"
      min_lines: 80
    - path: "hooks/useArchetype.ts"
      provides: "useArchetype hook returning calculated archetype and resilience"
      exports: ["useArchetype"]
  key_links:
    - from: "hooks/useArchetype.ts"
      to: "data/archetypes.ts"
      via: "import and invoke calculateArchetype"
      pattern: "calculateArchetype"
    - from: "hooks/useArchetype.ts"
      to: "App.tsx (later)"
      via: "called when transitioning to DEBRIEF_PAGE_1"
      pattern: "useArchetype"

---

<objective>
Create the foundational data structures and hooks for the archetype system: type definitions, archetype mappings, and the calculation logic that converts decision history into personality types.

Purpose: Wave 0 provides the data layer that debrief UI components will consume. This ensures archetype calculation is deterministic, testable, and happens once (not repeatedly on render).

Output: Archetype type definitions, 6-8 archetype definitions with descriptions and metadata, decision vector scoring function, resilience score calculation, and a reusable useArchetype hook.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-debrief-and-replay-system/06-ARCHITECTURE.md
@.planning/phases/06-debrief-and-replay-system/06-RESEARCH.md
@types.ts
@data/index.ts
@hooks/index.ts
@App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define archetype types and data structures</name>
  <files>types.ts</files>
  <behavior>
    - Archetype enum ArchetypeId with 6 values (PRAGMATIST, SHADOW_ARCHITECT, DISRUPTOR, CONSERVATIVE, BALANCED, CHAOS_AGENT)
    - Archetype interface with: id, name, description, icon, color, traits[]
    - DebriefState interface with: page (DebrieRStage enum), archetype, resilience, deathType
    - DebrieRStage enum: PAGE_1, PAGE_2, PAGE_3 (not added to GameStage yet — Wave 1 task)
  </behavior>
  <action>Extend types.ts with archetype and debrief-specific types. Define ArchetypeId as a string union enum with 6 archetype names. Create Archetype interface (id, name, description, icon, color, traits: string[]). Create DebriefState interface with current page (enum), calculated archetype, resilience score (0-100), and inherited death info. Create DebrieRStage enum for the 3-page debrief sequence (PAGE_1, PAGE_2, PAGE_3). These types support Wave 1 components and the archetype calculation system.</action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>types.ts includes ArchetypeId enum (6 values), Archetype interface, DebriefState interface, DebrieRStage enum. No type errors. File exports all types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create archetype mapping and scoring logic</name>
  <files>data/archetypes.ts, data/index.ts</files>
  <behavior>
    - ARCHETYPES object maps 6 ArchetypeId values to full Archetype definitions
    - calculateArchetype() takes: decision history, final budget/heat/hype, role → returns {archetype, resilience}
    - Scoring: for each decision, increment trait counters based on outcome penalties (fine < threshold → PRAGMATIST, heat > threshold → SHADOW_ARCHITECT, etc.)
    - Resilience = (sum of all trait scores / decision count) normalized to 0-100, capped at 100
    - No decision history → default to BALANCED
    - Export through data/index.ts barrel
  </behavior>
  <action>Create new file data/archetypes.ts with: (1) ARCHETYPES map with 6 archetype definitions including name, description, icon (Font Awesome class), color (Tailwind class), and traits array. (2) Define mapOutcomeToTraits() function that takes an outcome and returns trait increments based on penalty thresholds. (3) Implement calculateArchetype() that: scans the decision history, looks up each card from ROLE_CARDS, extracts the outcome (LEFT or RIGHT), maps to traits, increments counters, finds highest-scoring archetype, calculates resilience as normalized consistency score. (4) Implement calculateResilienceScore() as helper. Export from data/index.ts.</action>
  <verify>
    <automated>bun run test -- unit/archetype.test.ts</automated>
  </verify>
  <done>data/archetypes.ts exports ARCHETYPES (6 items), calculateArchetype(), calculateResilienceScore(). Unit tests pass: archetype calculated correctly for sample histories, resilience score in 0-100 range, empty history returns BALANCED, no type errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create useArchetype hook</name>
  <files>hooks/useArchetype.ts, hooks/index.ts</files>
  <behavior>
    - Hook signature: useArchetype(history, finalBudget, finalHeat, finalHype, role) → {archetype, resilience}
    - Calls calculateArchetype once and returns result
    - Memoizes result to prevent recalculation on re-render
    - Returns null archetype if role is null (guard against invalid state)
  </behavior>
  <action>Create hooks/useArchetype.ts. Implement useArchetype hook that accepts decision history (from GameState), final budget/heat/hype, and role. Call calculateArchetype internally using useMemo to prevent re-calculation. Return {archetype, resilience}. Export from hooks/index.ts. Write tests verifying hook returns cached result on second call and handles edge cases (empty history, null role).</action>
  <verify>
    <automated>bun run test -- unit/archetype.test.ts --grep "useArchetype"</automated>
  </verify>
  <done>hooks/useArchetype.ts exports useArchetype hook. Hook called with valid inputs returns {archetype: Archetype, resilience: number}. Memoization works (same result on re-call). Edge cases handled. No type errors.</done>
</task>

<task type="auto">
  <name>Task 4: Create unit test file for Wave 0 archetype validation</name>
  <files>unit/archetype.test.ts</files>
  <action>Create unit/archetype.test.ts with vitest. Write tests for: (1) calculateArchetype() with sample decision histories — verify correct archetype is selected based on decision patterns. (2) Resilience score calculation — verify score is 0-100 and reflects decision consistency. (3) Edge cases: empty history returns BALANCED, null role returns null archetype. (4) mapOutcomeToTraits() correctly maps outcome penalties to trait increments. (5) useArchetype hook memoization and caching. Use ROLE_CARDS sample data for decision history construction.</action>
  <verify>
    <automated>bun run test -- unit/archetype.test.ts</automated>
  </verify>
  <done>unit/archetype.test.ts created with 8+ test cases covering archetype calculation, resilience scoring, edge cases, and hook behavior. All tests pass.</done>
</task>

</tasks>

<verification>
After Wave 0 tasks complete:
1. Run `bun run typecheck` — no errors
2. Run `bun run test -- unit/archetype.test.ts` — all tests green
3. Verify data/index.ts exports ARCHETYPES, calculateArchetype, calculateResilienceScore
4. Verify hooks/index.ts exports useArchetype
5. Verify types.ts includes ArchetypeId, Archetype, DebriefState, DebrieRStage
</verification>

<success_criteria>
- Types defined: ArchetypeId (6 values), Archetype interface, DebriefState, DebrieRStage
- Archetype system implemented: ARCHETYPES map (6 items), calculateArchetype() function, resilience score calculation
- useArchetype hook created and tested
- All unit tests passing
- No type errors
- Ready for Wave 1: debrief UI components can import and use archetype data
</success_criteria>

<output>
After completion, create `.planning/phases/06-debrief-and-replay-system/06-01-SUMMARY.md`
</output>
