---
phase: 06-debrief-and-replay-system
plan: 13
type: auto
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - vite.config.ts
autonomous: true

must_haves:
  truths:
    - "Email form submission works in development without proxy errors"
    - "API endpoint /api/v2-waitlist is accessible during local development"
  artifacts:
    - path: "vite.config.ts"
      provides: "Proper API routing configuration"
  key_links:
    - from: "/api/v2-waitlist"
      to: "api/v2-waitlist.ts handler"
      via: "Vite plugin or proxy configuration"

issue_reference:
  test: 4
  issue: "HTTP proxy error: /api/v2-waitlist AggregateError [ECONNREFUSED]"
---

<objective>
Fix email form submission: Resolve proxy error by configuring Vite to handle API routes properly.

Root cause: Vite config has proxy pointing to http://localhost:3001 but no server is running there. The API endpoint (api/v2-waitlist.ts) is a Vercel serverless function that needs special handling.

Fix: Create a Vite plugin that handles /api/* routes by executing the API handlers directly during development.
</objective>

<files>
- vite.config.ts
- api/v2-waitlist.ts
</files>

<implementation_notes>
Current vite.config.ts has:
```typescript
proxy: {
  "/api": {
    target: "http://localhost:3001",  // No server here!
    changeOrigin: true,
  },
}
```

Need to replace this with a custom plugin that:
1. Intercepts requests to /api/* 
2. Imports and executes the appropriate handler from api/ directory
3. Returns the response

This allows the Vercel serverless functions to work during Vite dev without needing a separate server.
</implementation_notes>

<verify>
1. Start dev server with bun dev
2. Navigate to debrief Page 3
3. Fill in email and click Send
4. Verify no proxy error and form submits successfully
</verify>
