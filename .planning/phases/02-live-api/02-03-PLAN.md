---
phase: 02-live-api
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Roast.exe uses streaming audio when Live API available"
    - "Roast.exe falls back to standard TTS when Live API fails"
    - "API key never exposed in browser code"
  artifacts:
    - path: "services/roastService.ts"
      provides: "Roast service with streaming audio and fallback logic"
      exports: ["getRoastWithStreaming", "getRoastWithFallback"]
  key_links:
    - from: "services/roastService.ts"
      to: "hooks/useLiveAudio"
      via: "useLiveAudio hook"
      pattern: "startLiveSession"
    - from: "services/roastService.ts"
      to: "services/geminiService"
      via: "speak function"
      pattern: "speak.*fallback"
---

<objective>
Integrate streaming audio into Roast.exe workflow with fallback to standard TTS.

Purpose: Enable real-time streaming audio in Roast.exe while maintaining reliability through fallback.

Output: Updated roast service with streaming and fallback support.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-live-api/02-01-SUMMARY.md
@.planning/phases/02-live-api/02-02-SUMMARY.md
@services/geminiService.ts
@hooks/useLiveAudio.ts

# Reference:
# - Current getRoast() in geminiService.ts generates text
# - Current speak() in geminiService.ts plays TTS
# - useLiveAudio hook provides streaming playback
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create roast service with streaming and fallback</name>
  <files>services/roastService.ts</files>
  <action>
Create service at `services/roastService.ts` that combines text generation with streaming audio and fallback.

Implementation must include:
1. Import existing getRoast() and speak() from geminiService.ts
2. Import useLiveAudio hook logic (or create client-side wrapper)
3. Create getRoastWithStreaming(prompt, personality) that:
   - First calls getRoast() to get the text
   - Then uses Live API to stream audio of that text
   - Or uses the existing speak() but AFTER getting first few tokens (not waiting for full)
4. Create getRoastWithFallback(prompt, personality) that:
   - Attempts streaming audio via Live API
   - Catches errors (connection failure, rate limit 429, timeout)
   - Falls back to standard TTS via speak()
   - Logs warning when fallback occurs: "Live API unavailable, falling back to standard TTS"
5. Handle personality-specific voice names:
   - ROASTER: 'Kore' (or similar deep voice)
   - ZEN_MASTER: 'Puck' (or similar calm voice)
   - LOVEBOMBER: 'Enceladus' (or similar energetic voice)

Error types that should trigger fallback:
- WebSocket connection errors
- 429 rate limit errors
- Timeout errors (>10 seconds)
- API key errors (401)
  </action>
  <verify>
Service file created with:
- getRoastWithStreaming function
- getRoastWithFallback function with proper error handling
- Personality-based voice configuration
</verify>
  <done>
Roast service created with streaming audio support and automatic fallback to standard TTS
</done>
</task>

<task type="auto">
  <name>Task 2: Update Roast.exe to use streaming service</name>
  <files>App.tsx</files>
  <action>
Update the Roast.exe workflow in App.tsx to use the new streaming service.

1. Find where getRoast() and speak() are currently called
2. Replace with getRoastWithFallback() or getRoastWithStreaming()
3. Ensure personality is passed correctly
4. Add UI feedback showing "Streaming..." vs "Loading..." states
5. Keep existing error handling

If getRoastWithStreaming is used:
- Show streaming indicator while audio plays
- Audio should start playing before text is fully displayed
- Update text display as audio streams (if Gemini provides partial text)

If getRoastWithFallback is used:
- Works identically to current but with automatic fallback
- Falls back silently to standard TTS on errors
  </action>
  <verify>
App.tsx updated to use new roast service
</verify>
  <done>
Roast.exe now uses streaming audio with automatic fallback to standard TTS
</done>
</task>

</tasks>

<verification>
Roast.exe functionality preserved with streaming audio as primary and fallback to TTS
</verification>

<success_criteria>
1. Streaming audio works in Roast.exe when Live API available
2. Falls back to standard TTS when Live API fails
3. User experience is smooth (no visible breakage on fallback)
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-api/02-03-SUMMARY.md`
</output>
