---
phase: 02-live-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup:
  - service: google-gemini
    why: "Gemini Live API for real-time streaming audio"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio -> API Keys"
      - name: GEMINI_API_KEY
        source: "Also add to .env.local for backend proxy"

must_haves:
  truths:
    - "Backend proxy protects API key from browser exposure"
    - "AudioWorklet correctly converts 24kHz to 48kHz (audio plays at normal speed)"
  artifacts:
    - path: "services/geminiLiveProxy.ts"
      provides: "Backend API route for secure Gemini Live API access"
      exports: ["POST /api/gemini/live"]
    - path: "public/audio-processor.worklet.js"
      provides: "AudioWorklet for streaming PCM playback with sample rate conversion"
  key_links:
    - from: "services/geminiLiveProxy.ts"
      to: "Gemini Live API"
      via: "WebSocket connection"
      pattern: "wss://.*generativelanguage.*"
    - from: "public/audio-processor.worklet.js"
      to: "AudioContext"
      via: "AudioWorkletNode"
      pattern: "AudioWorkletProcessor.*resample24to48"
---

<objective>
Create backend proxy for Gemini Live API and AudioWorklet processor for streaming playback.

Purpose: Secure API key (cannot be in browser) and implement sample rate conversion (24kHz→48kHz) to prevent audio from playing at double speed.

Output: Backend API route and AudioWorklet processor file.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-live-api/02-RESEARCH.md
@services/geminiService.ts

# Current TTS implementation to understand:
# - Uses gemini-2.5-flash-preview-tts
# - AudioContext created at 24kHz
# - Waits for full response before playing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend API proxy for Gemini Live API</name>
  <files>services/geminiLiveProxy.ts</files>
  <action>
Create a backend service that proxies requests to Gemini Live API, protecting the API key from browser exposure.

1. Create `services/geminiLiveProxy.ts` with:
   - POST endpoint accepting {prompt, personality}
   - Uses @google/genai SDK to connect to Gemini Live API (model: gemini-2.5-flash-native-audio-preview-12-2025)
   - Streams audio chunks back to client via WebSocket or server-sent events
   - Includes proper error handling for connection failures
   - NOTE: For now, create a service that can be called - actual server implementation depends on Vercel/Next.js API route availability

Alternatively, create a Next.js API route at `src/app/api/gemini/live/route.ts` if using Next.js.

Key implementation details from research:
- Use WebSocket (wss://...) for bidirectional streaming
- Handle base64-encoded PCM audio chunks from response.serverContent.inlineMedia.data
- Return chunks as they arrive for streaming playback
  </action>
  <verify>
File exists at services/geminiLiveProxy.ts or src/app/api/gemini/live/route.ts
</verify>
  <done>
Backend proxy service created that accepts prompts and streams audio chunks, protecting API key
</done>
</task>

<task type="auto">
  <name>Task 2: Create AudioWorklet processor with sample rate conversion</name>
  <files>public/audio-processor.worklet.js</files>
  <action>
Create AudioWorklet processor at `public/audio-processor.worklet.js` for streaming PCM audio playback with 24kHz→48kHz conversion.

Implementation must include:
1. Extend AudioWorkletProcessor class
2. Implement process() method that fills output with buffered audio
3. Implement addChunk() method receiving PCM data from main thread
4. Convert Int16 to Float32 (divide by 32768.0)
5. CRITICAL: Resample 24kHz→48kHz using linear interpolation:
   ```javascript
   function resample24to48(input) {
     const output = new Float32Array(input.length * 2);
     for (let i = 0; i < output.length; i++) {
       const srcIndex = i / 2;
       const srcIndexInt = Math.floor(srcIndex);
       const frac = srcIndex - srcIndexInt;
       output[i] = input[srcIndexInt] * (1 - frac) + input[Math.min(srcIndexInt + 1, input.length - 1)] * frac;
     }
     return output;
   }
   ```
6. Use MessagePort to receive audio chunks from main thread
7. Handle buffer management to prevent memory leaks (limit buffer size)

Register as: 'streaming-audio-processor'
  </action>
  <verify>
File exists at public/audio-processor.worklet.js and contains:
- AudioWorkletProcessor class
- resample24to48 function
- registerProcessor('streaming-audio-processor', ...)
</verify>
  <done>
AudioWorklet processor created that converts 24kHz PCM to 48kHz and streams to browser audio output
</done>
</task>

</tasks>

<verification>
Both files created and follow the architecture patterns from 02-RESEARCH.md
</verification>

<success_criteria>
1. Backend proxy created - API key protected from browser
2. AudioWorklet created - handles sample rate conversion correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-api/02-01-SUMMARY.md`
</output>
