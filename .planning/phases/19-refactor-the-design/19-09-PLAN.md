---
phase: 19-refactor-the-design
plan: "09"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/game/debrief/DebriefPage1Collapse.tsx
autonomous: true
gap_closure: true
requirements: [DESIGN-01]

must_haves:
  truths:
    - "Victory path uses h1 element (either for title or visually hidden) matching death path structure"
    - "Victory trophy icon respects prefers-reduced-motion: animate-pulse has motion-reduce:animate-none"
    - "Victory section uses same structural pattern as death section (header block, image block, content block)"
</objective>

<objective>
Align victory page (DebriefPage1Collapse) with death page patterns: add h1 for screen readers, add prefers-reduced-motion support, match structural hierarchy.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Gap source: User feedback after Phase 19 verification passed
- "Death path leads with an h1 (GAME OVER / Kirk). Victory leads with a decorative trophy and then h2 'Quarter survived' with no h1. That's a sloppy document outline"
- "prefers-reduced-motion: The victory trophy uses animate-pulse with no motion-reduce: override"
- "align victory to same pattern as gave over and kirk"
</context>

<tasks>

<task type="auto">
  <name>Add h1 to victory path in DebriefPage1Collapse</name>
  <files>components/game/debrief/DebriefPage1Collapse.tsx</files>
  <action>
In the victory section (lines 255-267), the death section uses h1 (line 126 "GAME OVER" in GameOverHeader). The victory section only has h2 "Quarter survived" (line 260).

Options:
1. Change h2 to h1 (simple but changes visual hierarchy)
2. Add visually hidden h1 for screen readers (sr-only)

Choose option 2 to maintain visual design while fixing screen reader issue. Add a visually hidden h1:
```
<h1 className="sr-only">Quarter Survived - Victory</h1>
```
Before or after the trophy div, wrapped in the fragment.

This matches the pattern of FeedbackOverlay line 141 which uses sr-only for "Governance feedback".
  </action>
  <verify>Typecheck passes. Screen reader can find h1 on victory page. HTML structure is valid.</verify>
  <done>Victory path has h1 element for screen reader accessibility</done>
</task>

<task type="auto">
  <name>Add prefers-reduced-motion to victory trophy</name>
  <files>components/game/debrief/DebriefPage1Collapse.tsx</files>
  <action>
Line 257 has: `animate-pulse drop-shadow-[0_0_30px_rgba(34,197,94,0.4)]`

Add motion-reduce variant. Tailwind pattern: `motion-reduce:animate-none` or use `@media (prefers-reduced-motion: reduce)` in CSS.

Add to the className:
- Option 1: `motion-reduce:animate-none` (if Tailwind motion plugin is installed)
- Option 2: Custom CSS class

Check if the starfield already uses reduced-motion pattern - reference that approach.
  </action>
  <verify>Violates no prefers-reduced-motion warnings. Trophy animation disabled when user has motion preferences set.</verify>
  <done>Victory trophy respects prefers-reduced-motion</done>
</task>

<task type="auto">
  <name>Align victory structure with death pattern</name>
  <files>components/game/debrief/DebriefPage1Collapse.tsx</files>
  <action>
Death path structure (lines 268-302):
- GameOverHeader with h1 (inside mb-6 md:mb-8 wrapper)
- Image block (Kirk: lines 277-285)
- DeathEndingCard (lines 287-292)
- Explanation block
- FailureLessonCard

Victory path structure (lines 255-267):
- Trophy icon div (no wrapper)
- h2
- Description p

Add a wrapper div around trophy + h2 to match the `mb-6 md:mb-8` wrapper pattern used in death path. This creates structural consistency:
```jsx
<div className="mb-6 md:mb-8">
  <div className="text-6xl...">...</div>
  <h2>...</h2>
  <p>...</p>
</div>
```
  </action>
  <verify>Victory section has wrapper div matching death section structure</verify>
  <done>Victory section structural pattern matches death section</done>
</task>

</tasks>

<verification>
- [ ] Victory path has h1 (sr-only or visible)
- [ ] Trophy icon respects prefers-reduced-motion
- [ ] Victory section wrapped in mb-6 md:mb-8 div like death section
- [ ] No screen reader "where's the page title?" issue on victory
</verification>

<success_criteria>
Victory page accessibility and structure matches death page patterns. Screen readers get proper h1. Motion-sensitive users see no pulsing animation.
</success_criteria>

<output>
After completion, create `.planning/phases/19-refactor-the-design/19-09-SUMMARY.md`
</output>
