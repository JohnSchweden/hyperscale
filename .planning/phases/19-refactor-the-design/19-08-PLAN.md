---
phase: 19-refactor-the-design
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/game/FeedbackOverlay.tsx
autonomous: true
gap_closure: true
requirements: [DESIGN-01]

must_haves:
  truths:
    - "FeedbackOverlay lesson block has a muted label above it: 'Learning moment' or similar"
    - "Label uses same styling as other micro-labels (text-[10px], opacity-70)"
</objective>

<objective>
Restore the learning lesson title/label to FeedbackOverlay that was removed during subtractive cleanup. The user wants a title above the lesson text block.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Gap source: User feedback after Phase 19 verification passed
- "I'm missing a title for the learning leccon text. add back here the previous title."
</context>

<tasks>

<task type="auto">
  <name>Add lesson title label to FeedbackOverlay</name>
  <files>components/game/FeedbackOverlay.tsx</files>
  <action>
In the lesson block (lines 171-199), add a muted micro-label above the lesson text similar to the teamImpact and realWorldReference blocks.

The label should say "Learning moment" or "Lesson" — use same styling as:
- Line 180: `text-[10px] font-bold tracking-wide text-amber-400/70` for Team impact
- Line 190: `text-[10px] font-bold tracking-wide text-cyan-400/70` for Real Case

Add it before line 175 where the lesson text starts, matching the pattern of teamImpact block.
  </action>
  <verify>Typecheck passes. Build succeeds. Visual inspection shows muted label above lesson text.</verify>
  <done>FeedbackOverlay lesson block has a muted "Learning moment" label above the lesson text</done>
</task>

</tasks>

<verification>
- [ ] Lesson block has micro-label above it
- [ ] Label uses consistent styling with other micro-labels (text-[10px], opacity-70)
- [ ] Label text is clear: "Learning moment" or "Lesson"
</verification>

<success_criteria>
FeedbackOverlay displays a labeled lesson section that matches the design pattern of other secondary info blocks.
</success_criteria>

<output>
After completion, create `.planning/phases/19-refactor-the-design/19-08-SUMMARY.md`
</output>
