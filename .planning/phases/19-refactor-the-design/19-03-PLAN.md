---
phase: 19-refactor-the-design
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/game/debrief/DebriefPage2AuditTrail.tsx
autonomous: true
requirements: [DESIGN-03]
must_haves:
  truths:
    - "The 'What would you do differently?' reflection block is entirely absent from the rendered output"
    - "The personality sign-off block still renders immediately before the CTA button"
    - "The audit log entries still render as before"
    - "typecheck passes"
  artifacts:
    - path: "components/game/debrief/DebriefPage2AuditTrail.tsx"
      provides: "DebriefPage2 without the reflection prompt block"
  key_links:
    - from: "DebriefPage2AuditTrail.tsx"
      to: "CTA button"
      via: "personality sign-off → Generate Psych Evaluation button"
      pattern: "Generate Psych Evaluation"
---

<objective>
Remove the "What would you do differently?" reflection prompt block from DebriefPage2AuditTrail.

Purpose: The block (lines 323–388) repeats content the player already processed via the audit log directly above it. The audit log entries show each choice + its alternative + consequence. The personality sign-off handles the emotional close. The block is pure filler that adds scroll length without new information.

Output: DebriefPage2 with target section flow: [header] → [audit log entries] → [personality sign-off] → [CTA]
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-refactor-the-design/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the reflection prompt block from DebriefPage2AuditTrail</name>
  <files>components/game/debrief/DebriefPage2AuditTrail.tsx</files>
  <action>
Delete the entire "What would you do differently?" block from DebriefPage2AuditTrail.tsx.

The block starts at the comment `{/* Reflection Prompt — hidden for Kirk path */}` (around line 323) and ends at the closing `</div>` and `)}` that close both the JSX block and the `!isKirk` conditional (around line 388).

The block to remove is:
```tsx
{/* Reflection Prompt — hidden for Kirk path */}
{!isKirk && (
  <div
    className={`mb-6 rounded-xl p-5 sm:p-6 md:mb-8 ${GLASS_PANEL_DEFAULT}`}
  >
    <h3 className="mb-3 text-left text-lg font-bold text-slate-200 sm:text-center">
      <i className="fa-solid fa-lightbulb text-yellow-500 mr-2"></i>
      What would you do differently?
    </h3>
    <p className="text-left text-slate-400 text-sm mb-4 leading-relaxed">
      Every choice you made shaped this outcome. As you look back at
      your decisions, consider the paths not taken and why you made the
      choices you did.
    </p>

    {/* Per-choice hints for both safe and risky decisions */}
    {history.length > 0 && (
      <div className="mt-8">
        <div className="text-base font-semibold text-slate-300 mb-3 flex items-center justify-center gap-2 pt-[24px]">
          <i className="fa-solid fa-route text-cyan-400"></i>
          Path you didn't take
        </div>
        <div className="space-y-1 text-left">
          {history.map((entry, index) => {
            ...
          })}
        </div>
      </div>
    )}

    {/* Personality closing line */}
    <div className="mt-4 pt-4 border-t border-slate-700/50">
      <p className="text-sm italic text-cyan-400/80 text-center">
        {personalityClosing}
      </p>
    </div>
  </div>
)}
```

After removing this block, check whether any of the following are now unused (only used inside the removed block) and delete them if so:
- `personalityClosing` variable
- `PathHint` sub-component (if defined inline or locally in this file)
- `isKirk` variable (check — it may be used elsewhere in the file for the Kirk path; only remove if truly unused)
- Any imports added solely for the removed block

Read the file top-to-bottom to identify which variables are now unreferenced, then remove them to keep the linter clean.

Run `bun run typecheck` after changes.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run typecheck</automated>
  </verify>
  <done>
    - "What would you do differently?" text is absent from the file
    - The PathHint component (if it was only used in the removed block) is absent
    - personalityClosing is absent if it was only used in the removed block
    - personality sign-off block (the cyan card with robot icon) is still present
    - Generate Psych Evaluation button is still present
    - typecheck exits 0
  </done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes with 0 errors
- `grep "What would you do differently" components/game/debrief/DebriefPage2AuditTrail.tsx` returns nothing
- `grep "PathHint" components/game/debrief/DebriefPage2AuditTrail.tsx` returns nothing (if PathHint was local to this file)
- `grep "Generate Psych Evaluation" components/game/debrief/DebriefPage2AuditTrail.tsx` returns the button line (still present)
- `grep "fa-robot" components/game/debrief/DebriefPage2AuditTrail.tsx` returns the personality sign-off block (still present)
</verification>

<success_criteria>
DebriefPage2 audit trail no longer contains the reflection prompt block. Page flows directly from personality sign-off to CTA. typecheck passes.
</success_criteria>

<output>
After completion, create `.planning/phases/19-refactor-the-design/19-03-SUMMARY.md`
</output>
