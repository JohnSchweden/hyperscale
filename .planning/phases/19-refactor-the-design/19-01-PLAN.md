---
phase: 19-refactor-the-design
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/game/FeedbackOverlay.tsx
autonomous: true
requirements: [DESIGN-01]
must_haves:
  truths:
    - "The personality name label above the quote is gone — the quote renders without a header"
    - "The 'Governance alert' section header and its bottom border divider are removed"
    - "The 'Decision logged — no undo. Proceed when ready.' line is gone"
    - "The desktop image max-height is capped at 220px (md:max-h-[220px]) not uncapped"
    - "teamImpact and realWorldReference render in one unlabeled secondary block — no sub-headers"
    - "typecheck passes"
  artifacts:
    - path: "components/game/FeedbackOverlay.tsx"
      provides: "Simplified feedback overlay with subtractive changes only"
  key_links:
    - from: "FeedbackOverlay.tsx"
      to: "lesson + teamImpact + realWorldReference"
      via: "merged unlabeled block in id=feedback-overlay-desc"
      pattern: "feedback-overlay-desc"
---

<objective>
Remove visual clutter from FeedbackOverlay to match the minimalist design DNA of the selection screens.

Purpose: FeedbackOverlay currently has a labeled sub-section header ("Governance alert"), a redundant personality attribution label, a noise-adding "Decision logged" line, an uncapped desktop image, and two labeled sub-sections (Team impact / Real Case) competing for attention. All five are removed or simplified.

Output: FeedbackOverlay with target section flow: [image] → [icon + fine] → [quote] → [lesson + reference (unlabeled)] → [button]
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-refactor-the-design/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 5 clutter elements from FeedbackOverlay</name>
  <files>components/game/FeedbackOverlay.tsx</files>
  <action>
Make the following subtractive changes to components/game/FeedbackOverlay.tsx:

1. **Line 153 — Remove personality name label:** Delete the entire div:
   ```tsx
   <div className="text-cyan-400 mono text-xs md:text-sm mb-3 md:mb-4 font-bold tracking-wide">
     {personalityName}'s review
   </div>
   ```
   The quote that follows speaks for itself.

2. **Line 164 — Remove "Governance alert" sub-section header:** Delete the div containing the "Governance alert" text and its `border-b` divider:
   ```tsx
   <div className="text-xs md:text-sm font-bold text-slate-300 tracking-wide mb-3 border-b border-white/5 pb-2">
     Governance alert
   </div>
   ```

3. **Line 192 — Remove "Decision logged" line:** Delete the paragraph:
   ```tsx
   <p className="mt-3 text-xs text-slate-500 font-medium">
     Decision logged — no undo. Proceed when ready.
   </p>
   ```

4. **Line 121 — Cap image height on desktop:** Change `containerClassName`:
   - Before: `containerClassName="max-h-[200px] md:max-h-none"`
   - After: `containerClassName="max-h-[200px] md:max-h-[220px]"`

5. **Lines ~170–191 — Merge teamImpact and realWorldReference into one unlabeled block:** Currently each sub-section has its own label ("Team impact", "Real Case: ..."). Remove both labels. Keep the separator `border-t border-white/5` between sections for visual breathing room but remove the bold label divs. The content paragraphs remain.

   For teamImpact block — remove the label div:
   ```tsx
   <div className="text-xs font-bold text-amber-400/90 tracking-wide mb-1">
     Team impact
   </div>
   ```

   For realWorldReference block — remove the label div (the incident name + date):
   ```tsx
   <div className="text-xs font-bold text-cyan-400/90 tracking-wide mb-2">
     <i className="fa-solid fa-book-open mr-1.5" aria-hidden></i>
     Real Case: {realWorldReference.incident} ({realWorldReference.date})
   </div>
   ```

After all changes, run `bun run typecheck` to confirm no type errors.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run typecheck</automated>
  </verify>
  <done>
    - personalityName label line is absent from the file
    - "Governance alert" header div is absent from the file
    - "Decision logged" paragraph is absent from the file
    - containerClassName uses md:max-h-[220px] not md:max-h-none
    - "Team impact" and "Real Case:" label divs are absent — content paragraphs remain
    - typecheck exits 0
  </done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes with 0 errors
- `grep -n "personalityName" components/game/FeedbackOverlay.tsx` — the `{personalityName}'s review` div is absent (personalityName variable itself may remain defined but unused; if linter flags it, remove the variable too)
- `grep "Governance alert" components/game/FeedbackOverlay.tsx` returns nothing
- `grep "Decision logged" components/game/FeedbackOverlay.tsx` returns nothing
- `grep "md:max-h-none" components/game/FeedbackOverlay.tsx` returns nothing
- `grep "Team impact" components/game/FeedbackOverlay.tsx` returns nothing
</verification>

<success_criteria>
FeedbackOverlay renders the five removed elements no longer visible. The overlay content flows: escalation banner (if applicable) → image (if available, capped 220px) → icon → fine block → quote → lesson+reference block → Next ticket button. typecheck passes.
</success_criteria>

<output>
After completion, create `.planning/phases/19-refactor-the-design/19-01-SUMMARY.md`
</output>
