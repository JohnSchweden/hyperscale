---
phase: 19-refactor-the-design
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/game/FeedbackOverlay.tsx
autonomous: true
gap_closure: true
requirements: [DESIGN-01]

must_haves:
  truths:
    - "FeedbackOverlay displays the Hype metric alongside Budget and Heat when hype data is available"
    - "Hype metric uses similar styling to Heat (amber/red coloring, icon prefix)"
    - "Hype value displays correctly: 0-100 with appropriate color thresholds"
</objective>

<objective>
Add hype metric display to FeedbackOverlay to complete the trio of metrics (Budget, Heat, Hype) that users see throughout the game.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Gap source: User feedback after Phase 19 verification passed
- "in feedback overlay i see only the fine but not the impact on risk and hype"
</context>

<tasks>

<task type="auto">
  <name>Add hype metric display to FeedbackOverlay</name>
  <files>components/game/FeedbackOverlay.tsx</files>
  <action>
Add hype display to the escalation section (lines 99-120). Currently shows Budget Critical and Heat Critical/High but not Hype. Add:

1. Accept `hype` prop (number | undefined) in FeedbackOverlayProps (already passed from parent based on game state)
2. Add hypeCritical threshold (similar to heatCritical = 85) - if hype > 85, show critical
3. Add display in the escalation block next to budget/heat, using similar styling:
   - If hype >= 85: red, "Hype Critical"
   - If hype >= 70: amber, "Hype High"  
   - Use fa-bullhorn icon for hype

Reference StatsGrid component in DebriefPage1Collapse for color thresholds (line 45: hype uses text-cyan-400).
  </action>
  <verify>Typecheck passes. Build succeeds. Check FeedbackOverlay shows hype in escalation section when hype data is available.</verify>
  <done>FeedbackOverlay displays Hype metric in escalation section alongside Budget and Heat</done>
</task>

</tasks>

<verification>
- [ ] FeedbackOverlay accepts hype prop
- [ ] Hype displays in escalation bar with appropriate icon (fa-bullhorn)
- [ ] Color thresholds: hype >= 85 = red (critical), hype >= 70 = amber (high)
- [ ] Matches visual style of existing Budget/Heat displays
</verification>

<success_criteria>
FeedbackOverlay shows all three metrics (Budget, Heat, Hype) in the escalation section when game state includes hype data.
</success_criteria>

<output>
After completion, create `.planning/phases/19-refactor-the-design/19-07-SUMMARY.md`
</output>
