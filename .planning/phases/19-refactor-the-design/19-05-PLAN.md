---
phase: 19-refactor-the-design
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/game/debrief/DebriefPage3Verdict.tsx
autonomous: true
requirements: [DESIGN-05]
must_haves:
  truths:
    - "The 'Endings discovered: X/6 ...or is it?' block is absent from DebriefPage3"
    - "typecheck passes"
  artifacts:
    - path: "components/game/debrief/DebriefPage3Verdict.tsx"
      provides: "DebriefPage3 without the duplicate endings count hint"
  key_links:
    - from: "DebriefPage3Verdict.tsx"
      to: "endings count hint"
      via: "absent — duplicate of GameOver screen"
      pattern: "Endings discovered"
---

<objective>
Remove the "Endings discovered: X/6 ...or is it?" hint from DebriefPage3Verdict.

Purpose: This hint (lines 279–284) duplicates information already displayed on the GameOver screen. Showing it again on the verdict page adds noise without value.

Output: DebriefPage3 with the endings hint block removed. The LinkedIn CTA remains as the final element.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-refactor-the-design/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove endings hint from DebriefPage3Verdict</name>
  <files>components/game/debrief/DebriefPage3Verdict.tsx</files>
  <action>
Delete the endings discovered hint block from components/game/debrief/DebriefPage3Verdict.tsx.

The block to remove (around lines 278–284):
```tsx
{/* "...or is it?" hint for completionists — shown to all players */}
<div className="mt-6 text-center">
  <p className="text-sm text-slate-500">
    Endings discovered: {unlockedEndingsCount}/6{" "}
    <span className="italic text-slate-600 text-xs">...or is it?</span>
  </p>
</div>
```

After removing this block, check whether `unlockedEndingsCount` is used anywhere else in the file. If it is only used in this removed block:
- Remove it from the component's variable declarations / destructuring
- If it came from a hook call (e.g. `useUnlockedEndings`), remove the destructured variable. If removing it leaves the hook call with no destructured values and the hook is no longer needed, remove the hook call entirely.
- Remove any now-unused import if applicable.

Run `bun run typecheck` after changes.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run typecheck</automated>
  </verify>
  <done>
    - "Endings discovered" text is absent from the file
    - "...or is it?" text is absent from the file
    - unlockedEndingsCount variable is absent if it was only used in the removed block
    - typecheck exits 0
  </done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes with 0 errors
- `grep "Endings discovered" components/game/debrief/DebriefPage3Verdict.tsx` returns nothing
- `grep "or is it" components/game/debrief/DebriefPage3Verdict.tsx` returns nothing
</verification>

<success_criteria>
DebriefPage3 verdict page no longer shows the endings discovered hint. The LinkedIn CTA is the final element. typecheck passes.
</success_criteria>

<output>
After completion, create `.planning/phases/19-refactor-the-design/19-05-SUMMARY.md`
</output>
