---
phase: "02-new-role-set-impact-zones"
plan: "01"
type: TDD
wave: 1
depends_on: []
files_modified:
  - "types.ts"
  - "data/roles.ts"
  - "data/cards/index.ts"
  - "hooks/useGameState.ts"
  - "components/game/InitializingScreen.tsx"
  - "components/game/RoleSelect.tsx"
autonomous: true
must_haves:
  truths:
    - "Players can start runs with the new impact-zone roles and never see legacy roles as active choices."
    - "Every new impact zone resolves to a valid existing card deck, so role selection still leads into a playable run."
    - "Role-dependent runtime behavior still works after the legacy enum members are retired from active use."
  artifacts:
    - path: "types.ts"
      provides: "New RoleType enum values with legacy members preserved as comments"
      contains: "export enum RoleType"
    - path: "data/roles.ts"
      provides: "Impact-zone metadata, descriptions, labels, icons, and temporary legacy deck aliases"
      exports: ["ROLE_DESCRIPTIONS", "ROLE_LABELS", "ROLE_ICONS", "ROLE_DECK_ALIASES"]
    - path: "data/cards/index.ts"
      provides: "ROLE_CARDS keyed by the new impact-zone roles"
      exports: ["ROLE_CARDS"]
    - path: "hooks/useGameState.ts"
      provides: "Death-type branching derived from temporary deck aliases instead of removed legacy enums"
      contains: "determineDeathType"
    - path: "components/game/InitializingScreen.tsx"
      provides: "Initializing copy that renders impact-zone labels from shared role metadata"
      exports: ["InitializingScreen"]
    - path: "components/game/RoleSelect.tsx"
      provides: "Role selection grid wired to ROLE_LABELS/ROLE_ICONS for compile"
      exports: ["RoleSelect"]
  key_links:
    - from: "data/roles.ts"
      to: "data/cards/index.ts"
      via: "temporary deck alias record consumed by ROLE_CARDS"
      pattern: "ROLE_DECK_ALIASES"
    - from: "data/roles.ts"
      to: "hooks/useGameState.ts"
      via: "legacy-deck grouping used for death ending logic"
      pattern: "ROLE_DECK_ALIASES|getLegacyDeck"
    - from: "data/roles.ts"
      to: "components/game/InitializingScreen.tsx"
      via: "shared label metadata instead of enum prettifying"
      pattern: "ROLE_LABELS|getRoleLabel"
    - from: "data/roles.ts"
      to: "components/game/RoleSelect.tsx"
      via: "ROLE_LABELS and ROLE_ICONS for compile-safe role presentation"
      pattern: "ROLE_LABELS|ROLE_ICONS"
---

<objective>
Replace the active role model with the new impact-zone set while keeping the old departments preserved in comments for later recovery.

Purpose: Give Phase 02 a stable data/runtime foundation so the new role labels work everywhere before the UI copy and tests are updated.
Output: New `RoleType` values, shared impact-zone metadata, alias-driven card routing, and runtime consumers updated to the new source of truth.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@types.ts
@data/roles.ts
@data/cards/index.ts
@hooks/useGameState.ts
@components/game/InitializingScreen.tsx
</context>

<tdd_config>
Use `execute`. This phase is mostly enum/data wiring plus UI glue; targeted regression checks are a better fit than a standalone RED/GREEN plan.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Replace active RoleType values and preserve the legacy enum inline</name>
  <files>types.ts</files>
  <action>
    Replace the active `RoleType` members with these impact-zone IDs:
    `CHIEF_SOMETHING_OFFICER`, `HEAD_OF_SOMETHING`, `SOMETHING_MANAGER`, `TECH_AI_CONSULTANT`, `DATA_SCIENTIST`, `SOFTWARE_ARCHITECT`, `SOFTWARE_ENGINEER`, `VIBE_CODER`, `VIBE_ENGINEER`, `AGENTIC_ENGINEER`.

    Keep the old members in place as commented lines directly under the enum header so Phase 05 can restore or remap against the original names without digging through git history. Do not leave any active runtime references to the commented legacy members after this task.
  </action>
  <verify>`rg "CHIEF_SOMETHING_OFFICER|AGENTIC_ENGINEER|//.*DEVELOPMENT" types.ts`</verify>
  <done>`RoleType` exposes only the 10 new active values, and the six legacy values remain preserved as comments for later recovery.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared impact-zone metadata and temporary deck aliases</name>
  <files>data/roles.ts, data/cards/index.ts</files>
  <action>
    Refactor `data/roles.ts` from "descriptions only" into the single source of truth for impact-zone presentation and temporary card routing. Keep `ROLE_DESCRIPTIONS`, and add shared metadata exports for display labels and Font Awesome icons so UI files stop hard-coding ternary logic.

    Add a temporary deck-alias record that maps each new role to the closest existing deck until Phase 05 adds role-specific cards. Use this provisional mapping unless the code reveals a hard blocker:
    - `CHIEF_SOMETHING_OFFICER` -> `MANAGEMENT`
    - `HEAD_OF_SOMETHING` -> `MANAGEMENT`
    - `SOMETHING_MANAGER` -> `FINANCE`
    - `TECH_AI_CONSULTANT` -> `MARKETING`
    - `DATA_SCIENTIST` -> `HR`
    - `SOFTWARE_ARCHITECT` -> `DEVELOPMENT`
    - `SOFTWARE_ENGINEER` -> `DEVELOPMENT`
    - `VIBE_CODER` -> `DEVELOPMENT`
    - `VIBE_ENGINEER` -> `DEVELOPMENT`
    - `AGENTIC_ENGINEER` -> `DEVELOPMENT`

    Rewrite `ROLE_CARDS` in `data/cards/index.ts` so it derives values from that alias record instead of directly keying off the retired legacy role names. Preserve the old per-role mapping block as comments beside the new alias-driven implementation so future role-specific decks have immediate context.
  </action>
  <verify>`rg "ROLE_LABELS|ROLE_ICONS|ROLE_DECK_ALIASES" data/roles.ts && rg "ROLE_DECK_ALIASES|ROLE_CARDS" data/cards/index.ts`</verify>
  <done>Shared role metadata exists in one place, and every new impact zone points at a valid existing deck through a documented temporary alias layer.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire runtime consumers to the new role metadata</name>
  <files>hooks/useGameState.ts, components/game/InitializingScreen.tsx, components/game/RoleSelect.tsx</files>
  <action>
    Remove remaining assumptions that active roles are the old department enum members. In `hooks/useGameState.ts`, make death-ending branching derive from the temporary deck alias record (or a tiny helper built on it) so finance-, marketing-, and management-backed impact zones keep their special failure endings without hard-coding obsolete enum names. In `components/game/InitializingScreen.tsx`, replace the duplicated enum prettifier with the shared label metadata/helper so the countdown screen uses the new impact-zone names verbatim.

    In `components/game/RoleSelect.tsx`, replace `formatLabel` and the icon ternary with `ROLE_LABELS` and `ROLE_ICONS` from data/roles.ts so the component compiles after the legacy RoleType members are commented out. Do not change heading/copy here—Plan 02 updates that.

    Do not add a second copy of role labels or deck routing in these files; import the shared source of truth instead.
  </action>
  <verify>`bun run build`</verify>
  <done>Starting a run with any impact-zone role compiles cleanly, shows the correct countdown label, and preserves role-dependent gameplay branching through the temporary alias layer. RoleSelect uses shared metadata and no longer references removed legacy enum members.</done>
</task>

</tasks>

<verification>
Run the phase foundation checks after implementation:
- `bun run build`
- `bunx playwright test tests/stage-snapshots.spec.ts`

If the role-select snapshot fails only because approved Phase 02 UI text/icon changes landed, refresh the affected baseline in Plan 02 and rerun the file.
</verification>

<success_criteria>
- [ ] `RoleType` contains the 10 new impact-zone values and only commented legacy values for the old departments
- [ ] `data/roles.ts` exports shared labels, descriptions, icons, and temporary deck aliases
- [ ] `ROLE_CARDS` serves existing decks through the new alias mapping for all 10 roles
- [ ] `useGameState.ts` no longer branches on removed legacy enum members
- [ ] `InitializingScreen` shows the shared impact-zone label instead of prettified enum text
- [ ] `bun run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-new-role-set-impact-zones/02-01-SUMMARY.md`
</output>
