---
phase: "02-new-role-set-impact-zones"
plan: "02"
type: TDD
wave: 2
depends_on: ["01"]
files_modified:
  - "components/game/RoleSelect.tsx"
  - "tests/helpers/navigation.ts"
  - "tests/stage-snapshots.spec.ts"
autonomous: true
must_haves:
  truths:
    - "The role selection screen frames the choice as impact zones using the roadmap copy, not the old clearance-level language."
    - "Players see all 10 new impact-zone roles with their temporary descriptions/icons and no legacy role buttons."
    - "Regression helpers still reach the intended development-backed and marketing-backed gameplay paths using the renamed roles."
  artifacts:
    - path: "components/game/RoleSelect.tsx"
      provides: "Impact-zone selection grid with shared labels, descriptions, and icons"
      exports: ["RoleSelect"]
    - path: "tests/helpers/navigation.ts"
      provides: "Stable navigation helper that selects a development-backed impact zone"
      exports: ["navigateToPlaying"]
    - path: "tests/stage-snapshots.spec.ts"
      provides: "Updated snapshot/navigation coverage for the renamed role buttons"
      contains: "role-select"
  key_links:
    - from: "components/game/RoleSelect.tsx"
      to: "data/roles.ts"
      via: "shared labels, descriptions, and icons"
      pattern: "ROLE_LABELS|ROLE_DESCRIPTIONS|ROLE_ICONS"
    - from: "tests/helpers/navigation.ts"
      to: "components/game/RoleSelect.tsx"
      via: "visible role button text for a development-backed path"
      pattern: "Software Engineer"
    - from: "tests/stage-snapshots.spec.ts"
      to: "components/game/RoleSelect.tsx"
      via: "renamed role button text for development and marketing path coverage"
      pattern: "Software Engineer|Tech/AI Consultant"
---

<objective>
Ship the visible impact-zone role selection experience and update regression coverage to follow the renamed role buttons.

Purpose: Make Phase 02 user-facing instead of leaving the new role set hidden behind foundation work, while keeping the existing test paths aligned with the temporary deck aliases.
Output: Updated RoleSelect copy/layout plus regression helpers that navigate through the renamed roles successfully.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-new-role-set-impact-zones/02-01-PLAN.md
@components/game/RoleSelect.tsx
@tests/helpers/navigation.ts
@tests/stage-snapshots.spec.ts
</context>

<tdd_config>
Use `execute`. This plan is mostly UI copy, presentation wiring, and existing Playwright path updates.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy role-select framing with the Phase 02 impact-zone UI</name>
  <files>components/game/RoleSelect.tsx</files>
  <action>
    Update `RoleSelect.tsx` to match the roadmap brief exactly:
    - Heading: `Select your impact zone`
    - Copy: `Choose the specific silo you want to set on fire first. Each role changes the cascading failures, the legal heat you attract, and the creative ways to lose that $10M budget.`

    Render the 10 roles in the intended order from the shared metadata created in Plan 01. Remove the inline enum formatter and nested icon ternary; use the shared label, description, and icon exports instead. Keep deterministic `data-testid` values for each role button so test selectors remain stable after the copy refresh.
  </action>
  <verify>`rg "Select your impact zone|specific silo you want to set on fire first" components/game/RoleSelect.tsx`</verify>
  <done>`RoleSelect` shows only impact-zone roles, uses the approved Phase 02 copy verbatim, and reads presentation details from shared role metadata instead of duplicating logic inline.</done>
</task>

<task type="auto">
  <name>Task 2: Update regression navigation to use renamed roles and refresh role-select coverage</name>
  <files>tests/helpers/navigation.ts, tests/stage-snapshots.spec.ts</files>
  <action>
    Replace hard-coded legacy role selections with renamed roles that preserve the old gameplay coverage:
    - Use `Software Engineer` for the development-backed happy-path and boss-fight flow
    - Use `Tech/AI Consultant` for the marketing-backed bankruptcy path

    Update `navigateToPlayingFast` so its localStorage injection uses a valid new RoleType string: change `role: "development"` to `role: "SOFTWARE_ENGINEER"` (development-backed path). This ensures swipe-interactions.spec.ts and snap-back.spec.ts continue to reach the playing stage correctly after migration.

    Keep the remaining assertions intact so the tests still validate the same game behaviors after the rename. In `stage-snapshots.spec.ts`, refresh the role-select snapshot expectation if the new copy/icons legitimately change the screen, then rerun the file so the baseline reflects Phase 02 instead of the retired UI.
  </action>
  <verify>`bun run test -- tests/stage-snapshots.spec.ts`</verify>
  <done>Playwright helpers/tests navigate through renamed roles without depending on legacy button labels; navigateToPlayingFast injects SOFTWARE_ENGINEER for the development-backed path; the role-select snapshot coverage matches the approved Phase 02 UI.</done>
</task>

</tasks>

<verification>
Run the user-facing regression checks after implementation:
- `bun run build`
- `bun run test -- tests/stage-snapshots.spec.ts`
- `bun run test -- tests/voice-playback-integration.spec.ts`

If `stage-snapshots.spec.ts` reports only expected role-select visual diffs, update the generated role-select baseline and rerun until the file passes.
</verification>

<success_criteria>
- [ ] Role select heading/copy matches the roadmap brief exactly
- [ ] The UI renders all 10 new roles with shared labels, descriptions, and icons
- [ ] No legacy role button labels remain visible on the role-select screen
- [ ] `navigateToPlaying` uses a development-backed impact zone, not `Development`
- [ ] `stage-snapshots.spec.ts` covers the renamed development-backed and marketing-backed role paths
- [ ] Targeted Playwright regression checks pass after any approved snapshot refresh
</success_criteria>

<output>
After completion, create `.planning/phases/02-new-role-set-impact-zones/02-02-SUMMARY.md`
</output>
