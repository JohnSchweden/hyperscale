---
phase: 14-situational-outcome-imagery
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - components/game/debrief/DebriefContainer.tsx
  - tests/image-transitions.spec.ts
  - tests/image-cls.spec.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Debrief collapse page (DEBRIEF_PAGE_1) renders with death-type-specific images"
    - "Image test files use correct HTTPS protocol"
  artifacts:
    - path: "components/game/debrief/DebriefContainer.tsx"
      provides: "Renders DebriefPage1Collapse for DEBRIEF_PAGE_1 stage"
      exports: "DebriefPage1Collapse component"
    - path: "tests/image-transitions.spec.ts"
      provides: "HTTP protocol fix"
      contains: "https://localhost:3000"
    - path: "tests/image-cls.spec.ts"
      provides: "HTTP protocol fix"
      contains: "https://localhost:3000"
  key_links:
    - from: "DebriefContainer.tsx"
      to: "DebriefPage1Collapse"
      via: "import and case handler"
      pattern: "import.*DebriefPage1Collapse|case.*DEBRIEF_PAGE_1"
---

<objective>
Fix two gaps identified in UAT:
1. DebriefContainer.tsx missing DEBRIEF_PAGE_1 case handler (major)
2. Image test files using HTTP instead of HTTPS (minor)

This plan addresses the wiring bug that prevents death images from rendering on the game over page.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-situational-outcome-imagery/14-UAT.md
@components/game/debrief/DebriefContainer.tsx
@tests/image-transitions.spec.ts
@tests/image-cls.spec.ts

# Gap Summary from UAT:

## Gap 1 (Major): Missing DEBRIEF_PAGE_1 handler
- **Truth:** Debrief collapse page (DEBRIEF_PAGE_1) shows death-type-specific images
- **Root Cause:** DebriefContainer.tsx missing import and case handler
- **Missing:**
  - Import DebriefPage1Collapse in DebriefContainer.tsx
  - Add case GameStage.DEBRIEF_PAGE_1: return <DebriefPage1Collapse ... />

## Gap 2 (Minor): HTTP vs HTTPS
- **Truth:** Image test files use correct HTTPS protocol
- **Root Cause:** Tests use http://localhost:3000 instead of https://localhost:3000
- **Missing:** Change both files to use https://localhost:3000

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DEBRIEF_PAGE_1 case handler in DebriefContainer</name>
  <files>components/game/debrief/DebriefContainer.tsx</files>
  <action>
1. Add import for DebriefPage1Collapse at the top of the file:
   ```typescript
   import { DebriefPage1Collapse } from "./DebriefPage1Collapse";
   ```

2. Add case handler in the switch statement. The switch currently handles:
   - DEBRIEF_PAGE_2 → DebriefPage2AuditTrail
   - DEBRIEF_PAGE_3 → DebriefPage3Verdict
   - default → null

   Add this case BEFORE the default case:
   ```typescript
   case GameStage.DEBRIEF_PAGE_1:
     return <DebriefPage1Collapse state={state} onNext={onNextPage} />;
   ```

3. Verify the import uses the correct path: "./DebriefPage1Collapse" (same directory)
</action>
  <verify>
Run: bun run typecheck

Then verify by running collapse page tests:
- bun run test -- tests/image-collapse-page.spec.ts

Expected: Tests should no longer timeout waiting for GAME OVER element
  </verify>
  <done>
DebriefContainer.tsx imports DebriefPage1Collapse and renders it for DEBRIEF_PAGE_1 stage.
The switch statement now has 3 case handlers: DEBRIEF_PAGE_1, DEBRIEF_PAGE_2, DEBRIEF_PAGE_3.
</done>
</task>

<task type="auto">
  <name>Task 2: Fix HTTP to HTTPS in image-transitions.spec.ts</name>
  <files>tests/image-transitions.spec.ts</files>
  <action>
Change line 5 from:
  await page.goto("http://localhost:3000");
to:
  await page.goto("https://localhost:3000");

The test file uses baseURL from test.use() but explicitly navigates to http://. Change to https:// to match the dev server.
</action>
  <verify>
Run: bun run typecheck

Then verify tests can now connect:
- bun run test -- tests/image-transitions.spec.ts
  </verify>
  <done>
Test file now uses https://localhost:3000 protocol.
</done>
</task>

<task type="auto">
  <name>Task 3: Fix HTTP to HTTPS in image-cls.spec.ts</name>
  <files>tests/image-cls.spec.ts</files>
  <action>
Change line 5 from:
  await page.goto("http://localhost:3000");
to:
  await page.goto("https://localhost:3000");

The test file uses baseURL from test.use() but explicitly navigates to http://. Change to https:// to match the dev server.
</action>
  <verify>
Run: bun run typecheck

Then verify tests can now connect:
- bun run test -- tests/image-cls.spec.ts
  </verify>
  <done>
Test file now uses https://localhost:3000 protocol.
</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run typecheck: bun run typecheck (should pass)
2. Run collapse page tests: bun run test -- tests/image-collapse-page.spec.ts
   - Expected: "page renders with death ending card for BANKRUPT" should pass
   - Expected: "page renders with death ending card for KIRK" should pass
   - Expected: "death ending components render with image container" should pass

3. Run image transition tests: bun run test -- tests/image-transitions.spec.ts
   - Expected: All 4 tests should connect (no more ERR_EMPTY_RESPONSE)

4. Run CLS tests: bun run test -- tests/image-cls.spec.ts
   - Expected: All 4 tests should connect (no more ERR_EMPTY_RESPONSE)

5. Smoke test: bun run test:smoke
   - Expected: All tests pass (no regressions)
</verification>

<success_criteria>
- [ ] DebriefContainer.tsx imports DebriefPage1Collapse
- [ ] DebriefContainer.tsx has case handler for DEBRIEF_PAGE_1
- [ ] image-transitions.spec.ts uses https://localhost:3000
- [ ] image-cls.spec.ts uses https://localhost:3000
- [ ] All image-collapse-page tests pass
- [ ] All image-transition tests pass
- [ ] All image-cls tests pass
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-situational-outcome-imagery/14-06-SUMMARY.md`
</output>