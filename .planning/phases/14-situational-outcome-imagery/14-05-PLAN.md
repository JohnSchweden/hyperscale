---
phase: 14-situational-outcome-imagery
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - components/game/debrief/DebriefPage1Collapse.tsx
  - components/ImageWithFallback.tsx
autonomous: true
gap_closure: true
requirements:
  - IMAGE-03
  - IMAGE-05

must_haves:
  truths:
    - "KIRK deathType shows the /images/deaths/kirk.webp image on the Collapse page"
    - "ImageWithFallback placeholder uses the global .glitch-placeholder CSS class from index.html, not a duplicated inline <style> tag"
  artifacts:
    - path: "components/game/debrief/DebriefPage1Collapse.tsx"
      provides: "KIRK collapse image block rendered alongside the existing KIRK glitch header"
      contains: "getDeathImagePath(DeathType.KIRK)"
    - path: "components/ImageWithFallback.tsx"
      provides: "Placeholder div with className containing glitch-placeholder, no inline <style> tag"
  key_links:
    - from: "DebriefPage1Collapse.tsx isKirk branch"
      to: "ImageWithFallback"
      via: "getDeathImagePath(DeathType.KIRK)"
      pattern: "isKirk.*ImageWithFallback|ImageWithFallback.*KIRK"
    - from: "ImageWithFallback placeholder div"
      to: "index.html .glitch-placeholder rule"
      via: "className"
      pattern: "glitch-placeholder"
---

<objective>
Close two post-verification gaps in Phase 14: (1) the KIRK collapse image is never rendered because KIRK is excluded from the `hasRegularDeath` guard, and (2) the global `.glitch-placeholder` CSS class in `index.html` is dead code because `ImageWithFallback` duplicates the animation via an inline `<style>` tag instead of using the class.

Purpose: IMAGE-03 requires all 7 death types to show a collapse image including KIRK. The inline `<style>` duplication contradicts the intent of the global CSS established in plan 14-04.
Output: KIRK shows its collapse image; `ImageWithFallback` uses the global CSS class and removes the inline `<style>` block.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-situational-outcome-imagery/14-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render KIRK collapse image in DebriefPage1Collapse</name>
  <files>components/game/debrief/DebriefPage1Collapse.tsx</files>
  <action>
    In `DebriefPage1Collapse`, the `hasRegularDeath` guard correctly excludes KIRK from the `DeathEndingCard` component (which shows title, description, icon — none of which apply to KIRK). However, the image should still be displayed for KIRK.

    Add a KIRK-specific image block immediately after the `GameOverHeader` div (before the `deathEnding && deathType` block). The block should only render when `isKirk` is true:

    ```tsx
    {isKirk && (
      <div className="mb-6 md:mb-8 mx-auto max-w-md">
        <ImageWithFallback
          src={getDeathImagePath(DeathType.KIRK) ?? ""}
          alt="KIRK simulation breach"
          aspectRatio="video"
        />
      </div>
    )}
    ```

    `ImageWithFallback` and `getDeathImagePath` are already imported at the top of the file. `DeathType.KIRK` is already imported. No new imports needed.

    Do NOT touch `hasRegularDeath`, `deathEnding`, or any other logic — only add this new JSX block.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>
    When `deathType === DeathType.KIRK`, the collapse page renders an `ImageWithFallback` with `src="/images/deaths/kirk.webp"` alongside the existing glitch header. No change to the regular death flow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ImageWithFallback placeholder to global .glitch-placeholder class</name>
  <files>components/ImageWithFallback.tsx</files>
  <action>
    The placeholder `div` in `ImageWithFallback` currently uses an inline `style` prop (`animation: "glitch-scan 0.15s infinite"`) and an inline `<style>` tag at the bottom of the component. The global `.glitch-placeholder` CSS class in `index.html` (lines 613–667) already defines this animation including `prefers-reduced-motion` handling — but the class is never applied.

    Make two changes:

    1. Add `"glitch-placeholder"` to the placeholder `div`'s `className` string (the `div` at line 87). It already has many classes — append `glitch-placeholder` to the template string.

    2. Remove the `style` prop from that same `div` (the `style={{ backgroundImage: ..., animation: ... }}` object). The `backgroundImage` inline style is fine to keep — only remove the `animation` property from the style object since the CSS class now handles it. Keep `backgroundImage` in the style prop:
       ```tsx
       style={{
         backgroundImage: `repeating-linear-gradient(...)`,
       }}
       ```

    3. Remove the entire `<style>` block at the bottom of the component (lines 130–139 — the `@keyframes glitch-scan` inline injection). The keyframe is already defined globally in `index.html`.

    Do NOT change any other props, logic, or JSX structure.
  </action>
  <verify>
    <automated>bun run typecheck && grep -n "glitch-placeholder" /Users/yevgenschweden/swiperisk/components/ImageWithFallback.tsx && ! grep -n "@keyframes" /Users/yevgenschweden/swiperisk/components/ImageWithFallback.tsx</automated>
  </verify>
  <done>
    `ImageWithFallback` placeholder div has `glitch-placeholder` in its className. The inline `@keyframes glitch-scan` `<style>` block is gone. The global CSS rule in `index.html` is now the single source of truth for the glitch animation.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `bun run typecheck` passes with no errors
- `grep -n "glitch-placeholder" components/ImageWithFallback.tsx` returns a match (class applied)
- `grep -c "@keyframes" components/ImageWithFallback.tsx` returns 0 (inline style removed)
- `grep -n "getDeathImagePath(DeathType.KIRK)" components/game/debrief/DebriefPage1Collapse.tsx` returns a match
- `bun run test:area:layout` — existing collapse page tests pass (KIRK test at line 33 should now find the image container)
</verification>

<success_criteria>
- KIRK deathType renders `/images/deaths/kirk.webp` on the Collapse page via `ImageWithFallback`
- `ImageWithFallback` uses `className="... glitch-placeholder ..."` and has no inline `@keyframes` block
- `bun run typecheck` exits 0
- No regressions in existing layout tests
</success_criteria>

<output>
After completion, create `.planning/phases/14-situational-outcome-imagery/14-05-SUMMARY.md`
</output>
