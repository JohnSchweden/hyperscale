---
phase: 17-shuffle-aware-feedback-tts-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types.ts
  - lib/deck.ts
  - lib/feedbackAudioDirection.ts
  - lib/deck.test.ts
  - unit/feedbackAudioDirection.test.ts
autonomous: true
requirements:
  - FA-01

must_haves:
  truths:
    - "Shuffled deck cards record whether onLeft/onRight were swapped from authoring order"
    - "canonicalFeedbackAudioSide maps UI swipe to the side used for pre-baked feedback filenames when swapped"
  artifacts:
    - path: "types.ts"
      provides: "optional choiceSidesSwapped on Card"
    - path: "lib/deck.ts"
      provides: "shuffleDeck sets choiceSidesSwapped true/false on every card"
    - path: "lib/feedbackAudioDirection.ts"
      provides: "canonicalFeedbackAudioSide(card, uiDirection)"
    - path: "lib/deck.test.ts"
      provides: "swap sets flag; no swap clears flag"
    - path: "unit/feedbackAudioDirection.test.ts"
      provides: "invert table for swapped vs not"
  key_links:
    - from: "lib/deck.ts"
      to: "types.ts"
      via: "Card.choiceSidesSwapped"
---

<objective>
Establish data model and pure mapping so pre-baked `feedback_*_left|right` files align with authoring `onLeft`/`onRight` even after `shuffleDeck` swaps sides.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-shuffle-aware-feedback-tts-fixes/17-CONTEXT.md
@.planning/phases/17-shuffle-aware-feedback-tts-fixes/17-RESEARCH.md
@types.ts
@lib/deck.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Card flag + shuffleDeck</name>
  <files>types.ts, lib/deck.ts, lib/deck.test.ts</files>
  <behavior>
    - Extend `Card` with optional `choiceSidesSwapped?: boolean`.
    - After Fisher-Yates in `shuffleDeck`, for each card: if swap applied, set `choiceSidesSwapped: true`; else `choiceSidesSwapped: false`.
    - Tests: mock `Math.random` to force swap and no-swap paths; assert flag and that left/right payloads match expected swap.
  </behavior>
  <action>
    1. Add `choiceSidesSwapped?: boolean` to `Card` in `types.ts`.
    2. In `lib/deck.ts` after the position shuffle loop, iterate shuffled cards; on 50% branch assign swapped sides and `choiceSidesSwapped: true`, else copy card with `choiceSidesSwapped: false`.
    3. Extend `lib/deck.test.ts` with deterministic `Math.random` mocks covering swap and no-swap.
  </action>
  <done>
    - Every card emitted from `shuffleDeck` has boolean `choiceSidesSwapped`.
    - When swap runs, `onLeft`/`onRight` match swapped data and flag is `true`.
    - When swap does not run for a card, flag is `false` and sides match source.
  </done>
  <verify>
    <automated>bunx vitest run lib/deck.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: canonicalFeedbackAudioSide helper</name>
  <files>lib/feedbackAudioDirection.ts, unit/feedbackAudioDirection.test.ts</files>
  <behavior>
    - Export `canonicalFeedbackAudioSide` taking card with optional `choiceSidesSwapped` and UI direction; return canonical side for filename stem.
    - If `choiceSidesSwapped === true`, invert LEFT↔RIGHT; if false/undefined, return `ui`.
    - Unit tests: all four combinations.
  </behavior>
  <action>
    1. Add `lib/feedbackAudioDirection.ts` with pure function (no React imports).
    2. Add `unit/feedbackAudioDirection.test.ts` with table-driven cases: (swapped, ui) → expected canonical side.
  </action>
  <done>
    - `canonicalFeedbackAudioSide({ choiceSidesSwapped: true }, LEFT) === RIGHT` and symmetric cases hold.
    - `choiceSidesSwapped: false` and `undefined` behave as identity on UI direction.
  </done>
  <verify>
    <automated>bunx vitest run unit/feedbackAudioDirection.test.ts</automated>
  </verify>
</task>

</tasks>
