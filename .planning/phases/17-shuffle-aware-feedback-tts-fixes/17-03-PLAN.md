---
phase: 17-shuffle-aware-feedback-tts-fixes
plan: 03
type: execute
wave: 3
depends_on:
  - "17-02"
files_modified:
  - data/cards/head-of-something.ts
  - hooks/useVoicePlayback.ts
  - scripts/export-card-content-audit.ts
  - scripts/generate-hos-remaining.ts
  - services/geminiLive.ts
  - api/roast.ts
  - tests/voice-hos-critical-audio.spec.ts
  - unit/roastPromptCopy.test.ts
  - public/audio/voices/roaster/feedback/
autonomous: true
requirements:
  - FA-03
  - FA-04

must_haves:
  truths:
    - "Only one of the duplicate shadow-AI HoS cards remains in HEAD_OF_SOMETHING_CARDS"
    - "CRITICAL_HOS_CARDS and export script sets match remaining critical ids"
    - "Orphan feedback audio for removed card id is deleted from repo"
    - "Roast Live + API prompts encourage varied length/rhythm"
  artifacts:
    - path: "data/cards/head-of-something.ts"
      provides: "single shadow-enforcement scenario without duplicate id"
    - path: "hooks/useVoicePlayback.ts"
      provides: "CRITICAL_HOS_CARDS updated"
    - path: "services/geminiLive.ts"
      provides: "revised personality instructions"
    - path: "api/roast.ts"
      provides: "revised word-count / cadence instruction"
    - path: "unit/roastPromptCopy.test.ts"
      provides: "asserts old rigid cadence phrase removed / new guidance present"
  key_links:
    - from: "api/roast.ts"
      to: "Vercel roast handler"
      via: "generateContent prompt body"
    - from: "services/geminiLive.ts"
      to: "connectToLiveSession"
      via: "getPersonalityInstruction systemInstruction"
---

<objective>
Remove duplicate HoS content and orphan audio; relax roast generation prompts. Depends on voice wiring so QA hears correct clips before asset deletion.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-shuffle-aware-feedback-tts-fixes/17-CONTEXT.md
@data/cards/head-of-something.ts
@hooks/useVoicePlayback.ts
@tests/voice-hos-critical-audio.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate HoS card</name>
  <files>data/cards/head-of-something.ts</files>
  <behavior>
    - Per CONTEXT: remove `hos_shadow_ai_team_discovery` **or** `shadow_ai_hos_1` (default remove `hos_shadow_ai_team_discovery`, keep `shadow_ai_hos_1`).
    - Ensure deck still has acceptable card count for gameplay; adjust only if tests assert exact count.
  </behavior>
  <action>
    1. Delete chosen card object from `HEAD_OF_SOMETHING_CARDS` array.
    2. Run `bun run test:data` and fix any card-id references.
  </action>
  <done>
    - Grep `head-of-something.ts`: only one of the two duplicate ids remains.
  </done>
  <verify>
    <automated>bun run test:data</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Sync critical lists, scripts, tests, audio</name>
  <files>hooks/useVoicePlayback.ts, scripts/export-card-content-audit.ts, scripts/generate-hos-remaining.ts, tests/voice-hos-critical-audio.spec.ts, public/audio/voices/roaster/feedback/</files>
  <behavior>
    - Remove dropped id from `CRITICAL_HOS_CARDS` and any duplicate `Set` in `export-card-content-audit.ts`.
    - Update generation scripts that enumerate HoS ids.
    - Update `voice-hos-critical-audio.spec.ts` expected ids.
    - Delete `feedback_{removedId}_left|right.{opus,mp3}` (both extensions if present).
    - Grep repo for removed id; fix references (planning docs optional in same commit or follow-up).
  </behavior>
  <action>
    1. Patch sets and scripts; delete four audio files (or two if only one format) for removed id.
    2. Re-run voice file existence tests.
  </action>
  <done>
    - No references to removed card id in runtime code or critical-audio test list.
    - Opus/mp3 pairs for remaining critical ids still pass existence checks.
  </done>
  <verify>
    <automated>bun run test:area:audio</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Roast prompt cadence</name>
  <files>services/geminiLive.ts, api/roast.ts, unit/roastPromptCopy.test.ts</files>
  <behavior>
    - Replace rigid "1-3 sentences" with instruction to vary length and rhythm (per Cursor plan).
    - Adjust `/api/roast` word cap or rephrase so outputs are not forced into identical shape.
  </behavior>
  <action>
    1. Edit `getPersonalityInstruction` strings in `geminiLive.ts`.
    2. Edit prompt template in `api/roast.ts`.
    3. Add `unit/roastPromptCopy.test.ts`: `readFileSync` both files; expect the literal substring `1-3 sentences` is absent; expect each file still contains a documented replacement phrase (e.g. `vary` / `rhythm` — pick one stable token per file and assert).
  </action>
  <done>
    - Automated test green; prompts no longer mandate identical short triple-sentence shape.
  </done>
  <verify>
    <automated>bun run vitest:run -- unit/roastPromptCopy.test.ts</automated>
    <manual>Optional: manual roast smoke for subjective tone</manual>
  </verify>
</task>

</tasks>
