---
phase: 17-shuffle-aware-feedback-tts-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - App.tsx
  - hooks/useVoicePlayback.ts
  - components/game/FeedbackOverlay.tsx
autonomous: true
requirements:
  - FA-02

must_haves:
  truths:
    - "Feedback overlay carries the canonical side used for Roaster TTS triggers"
    - "useVoicePlayback uses canonical side, not raw UI swipe, for feedbackVoiceTrigger"
    - "Incident pressure sees the same card as CardStack during PLAYING"
  artifacts:
    - path: "App.tsx"
      provides: "effectiveDeck-based currentCard; voiceAudioSide in overlay state; applyChoice sets voiceAudioSide"
    - path: "hooks/useVoicePlayback.ts"
      provides: "feedbackVoiceSide option drives trigger resolution"
  key_links:
    - from: "App.tsx"
      to: "hooks/useVoicePlayback.ts"
      via: "feedbackVoiceSide prop"
    - from: "App.tsx"
      to: "lib/feedbackAudioDirection.ts"
      via: "canonicalFeedbackAudioSide before setFeedbackOverlay"
---

<objective>
Wire shuffle-aware audio side into the playing loop and align pressure/timer metadata with the shuffled deck card instance.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-shuffle-aware-feedback-tts-fixes/17-CONTEXT.md
@App.tsx
@hooks/useVoicePlayback.ts
@hooks/useIncidentPressure.ts
@components/game/GameScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: currentCard from effectiveDeck</name>
  <files>App.tsx</files>
  <behavior>
    - When `stage === PLAYING` and `role` set, `cards = state.effectiveDeck ?? ROLE_CARDS[state.role]`, `currentCard = cards[state.currentCardIndex] ?? null`.
    - Remove incorrect `ROLE_CARDS[state.role][state.currentCardIndex]`-only path for playing pressure.
  </behavior>
  <action>
    1. Locate `currentCard` derivation in `App.tsx`.
    2. Replace with `effectiveDeck ?? ROLE_CARDS[role]` indexing; guard null deck/role.
  </action>
  <done>
    - `useIncidentPressure` receives the same `Card` instance (including `choiceSidesSwapped`) as the one rendered in `CardStack`.
  </done>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Overlay + voice playback</name>
  <files>App.tsx, hooks/useVoicePlayback.ts</files>
  <behavior>
    - Import and call `canonicalFeedbackAudioSide(card, direction)` inside `applyChoice`.
    - Add `voiceAudioSide: "LEFT" | "RIGHT"` to `FeedbackOverlayState`; pass to `useVoicePlayback` as `feedbackVoiceSide` (or equivalent prop name).
    - In `useVoicePlayback`, use `feedbackVoiceSide ?? feedbackChoice` for `feedbackVoiceTrigger` when both card id and choice exist.
    - Keep `choice` on overlay as **UI swipe** for tests and debrief semantics.
  </behavior>
  <action>
    1. Extend overlay state type and `setFeedbackOverlay` payload in `applyChoice`.
    2. Add hook option; branch before `feedbackVoiceTrigger`.
    3. Ensure Roaster-only guard unchanged.
  </action>
  <done>
    - With a card where `choiceSidesSwapped === true`, choosing UI LEFT loads the audio file stem that matches the **authoring** side now shown on the left (inverted canonical side).
  </done>
  <verify>
    <automated>bun run test:smoke</automated>
    <automated>bun run test:area:audio</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: FeedbackOverlay / WebMCP if needed</name>
  <files>components/game/FeedbackOverlay.tsx, hooks/useWebMCPTools.ts, unit/feedbackOverlay.spec.tsx</files>
  <behavior>
    - Only change if something reads overlay shape; update unit tests if props change.
  </behavior>
  <action>
    1. Grep for `FeedbackOverlayState` / overlay props; adjust only if TypeScript requires new fields on child components.
  </action>
  <done>
    - `bun run typecheck` clean; FeedbackOverlay tests pass or unchanged with no prop drift.
  </done>
  <verify>
    <automated>bun run vitest:run -- unit/feedbackOverlay.spec.tsx</automated>
  </verify>
</task>

</tasks>
