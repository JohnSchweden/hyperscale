---
phase: 13-image-asset-pipeline
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/data/image-prompts.test.ts
  - tests/data/image-map.test.ts
  - tests/data/image-assets.test.ts
autonomous: true
requirements:
  - PIPELINE-01
  - PIPELINE-03
  - PIPELINE-04

must_haves:
  truths:
    - "Prompt library test validates all 4 categories have entries (incident, outcome, archetype, death)"
    - "Prompt library test validates each prompt has required fields (id, category, entityKey, prompt, aspectRatio)"
    - "Image map test validates all 6 DeathType enum values have entries"
    - "Image map test validates all 6 ArchetypeId values have entries"
    - "Image map test validates all paths start with /images/ and end with .webp"
    - "Image assets test validates every path in image map has a corresponding file in public/"
  artifacts:
    - path: "tests/data/image-prompts.test.ts"
      provides: "Prompt library completeness validation"
      min_lines: 40
    - path: "tests/data/image-map.test.ts"
      provides: "Image map type coverage and path format validation"
      min_lines: 50
    - path: "tests/data/image-assets.test.ts"
      provides: "Physical file existence validation for all mapped images"
      min_lines: 30
  key_links:
    - from: "tests/data/image-prompts.test.ts"
      to: "scripts/prompts/image-prompts.ts"
      via: "import IMAGE_PROMPTS"
      pattern: "import.*IMAGE_PROMPTS.*from"
    - from: "tests/data/image-map.test.ts"
      to: "data/imageMap.ts"
      via: "import mapping records"
      pattern: "import.*INCIDENT_IMAGES|OUTCOME_IMAGES|ARCHETYPE_IMAGES|DEATH_IMAGES"
    - from: "tests/data/image-assets.test.ts"
      to: "data/imageMap.ts"
      via: "import all image paths and check file existence"
      pattern: "existsSync"
---

<objective>
Create failing test scaffolding for the image asset pipeline (TDD Wave 0).

Purpose: Define contracts that the prompt library, image map, and generated assets must satisfy BEFORE implementation begins. Tests will fail (RED) until Wave 1 plans create the actual files.

Output: 3 Vitest test files in tests/data/ that validate prompt library completeness, image map type coverage, and physical file existence.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-image-asset-pipeline/13-CONTEXT.md
@.planning/phases/13-image-asset-pipeline/13-RESEARCH.md
@.planning/phases/13-image-asset-pipeline/13-VALIDATION.md

<interfaces>
<!-- Key types from codebase that tests must reference -->

From types.ts:
```typescript
export enum DeathType {
  BANKRUPT = "BANKRUPT",
  REPLACED_BY_SCRIPT = "REPLACED_BY_SCRIPT",
  CONGRESS = "CONGRESS",
  FLED_COUNTRY = "FLED_COUNTRY",
  PRISON = "PRISON",
  AUDIT_FAILURE = "AUDIT_FAILURE",
}

export type ArchetypeId =
  | "PRAGMATIST"
  | "SHADOW_ARCHITECT"
  | "DISRUPTOR"
  | "CONSERVATIVE"
  | "BALANCED"
  | "CHAOS_AGENT";
```

From data/index.ts (barrel pattern):
```typescript
export { ARCHETYPES, ... } from "./archetypes";
export { DEATH_ENDINGS } from "./deathEndings";
// imageMap.ts will be added here
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create prompt library and image map test files</name>
  <files>tests/data/image-prompts.test.ts, tests/data/image-map.test.ts, tests/data/image-assets.test.ts</files>
  <behavior>
    - image-prompts.test.ts:
      - Has entries for all 4 categories: incident, outcome, archetype, death
      - Has exactly 6 incident prompts (prompt-injection, model-drift, explainability, shadow-ai, copyright, general-dilemma)
      - Has 8-10 outcome prompts (budget-crater, pr-disaster, legal-hammer, team-revolt, audit-doom, career-over, regulatory-nuke, data-breach)
      - Has exactly 6 archetype prompts (one per ArchetypeId)
      - Has exactly 6 death prompts (one per DeathType)
      - Every prompt has non-empty: id, category, entityKey, prompt, aspectRatio, style
      - No duplicate IDs
      - Aspect ratios are valid (16:9, 1:1, 4:3, 3:4, 9:16)
    - image-map.test.ts:
      - INCIDENT_IMAGES has entries for all 6 IncidentCategory values
      - OUTCOME_IMAGES has entries for all OutcomeConsequenceType values
      - ARCHETYPE_IMAGES has entries for all 6 ArchetypeId values
      - DEATH_IMAGES has entries for all 6 DeathType values
      - All image paths start with "/images/" and end with ".webp"
      - No duplicate paths across all maps
    - image-assets.test.ts:
      - Every path referenced in imageMap has a corresponding file in public/ directory
      - All files are non-zero size
  </behavior>
  <action>
    Create three Vitest test files following the existing tests/data/ pattern (see card-structure.test.ts for style reference).

    1. `tests/data/image-prompts.test.ts`:
       - Import `IMAGE_PROMPTS` from `../../scripts/prompts/image-prompts` (the file won't exist yet — tests will fail to import)
       - Test: "has entries for all 4 categories" — filter by category, assert each has correct count
       - Test: "incident prompts cover all 6 categories" — check entityKeys match expected set
       - Test: "archetype prompts cover all 6 types" — check entityKeys match ArchetypeId values
       - Test: "death prompts cover all 6 types" — check entityKeys match DeathType values
       - Test: "every prompt has required fields" — iterate all, assert non-empty id, category, entityKey, prompt, aspectRatio, style
       - Test: "no duplicate IDs" — Set size equals array length
       - Test: "aspect ratios are valid" — each is one of 16:9, 1:1, 4:3, 3:4, 9:16

    2. `tests/data/image-map.test.ts`:
       - Import INCIDENT_IMAGES, OUTCOME_IMAGES, ARCHETYPE_IMAGES, DEATH_IMAGES from `../../data/imageMap`
       - Import DeathType from `../../types`
       - Import ArchetypeId type for reference
       - Test: "INCIDENT_IMAGES has all 6 categories" — check keys match IncidentCategory
       - Test: "OUTCOME_IMAGES has all consequence types" — check keys length >= 8
       - Test: "ARCHETYPE_IMAGES has all 6 archetypes" — check keys match all ArchetypeId values
       - Test: "DEATH_IMAGES has all 6 death types" — check keys match all DeathType values
       - Test: "all paths start with /images/ and end with .webp"
       - Test: "no duplicate paths across all maps"

    3. `tests/data/image-assets.test.ts`:
       - Import all maps from `../../data/imageMap`
       - Use `fs.existsSync` to check `path.join(process.cwd(), 'public', imagePath)` for every path in all 4 maps
       - Test: "all incident images exist on disk"
       - Test: "all outcome images exist on disk"
       - Test: "all archetype images exist on disk"
       - Test: "all death images exist on disk"
       - Test: "all image files are non-zero size" (use fs.statSync)

    All tests should be well-structured with describe blocks per category. Tests WILL FAIL at this stage — that is expected for TDD Wave 0.
  </action>
  <verify>
    <automated>bun vitest run tests/data/image-prompts.test.ts tests/data/image-map.test.ts tests/data/image-assets.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>3 test files exist, all tests fail (RED) because source files don't exist yet. Test structure validates the contracts defined in VALIDATION.md.</done>
</task>

</tasks>

<verification>
- All 3 test files created in tests/data/
- Tests reference the correct import paths (scripts/prompts/image-prompts, data/imageMap)
- Tests cover all requirement IDs: PIPELINE-01 (prompts), PIPELINE-03 (file paths), PIPELINE-04 (image map)
- Running tests produces failures (not errors due to bad test syntax)
</verification>

<success_criteria>
- 3 test files exist with comprehensive assertions
- Tests fail with import/module-not-found errors (expected for Wave 0)
- Test contracts match VALIDATION.md requirements
</success_criteria>

<output>
After completion, create `.planning/phases/13-image-asset-pipeline/13-00-SUMMARY.md`
</output>
