---
phase: 13-image-asset-pipeline
plan: 01
type: execute
wave: 1
depends_on:
  - "13-00"
files_modified:
  - scripts/prompts/image-prompts.ts
  - data/imageMap.ts
  - data/index.ts
autonomous: true
requirements:
  - PIPELINE-01
  - PIPELINE-03
  - PIPELINE-04

must_haves:
  truths:
    - "Prompt library contains ~26-28 typed prompts across 4 categories"
    - "Every incident category has a prompt with photorealistic uncanny-valley art direction"
    - "Every outcome consequence type has a prompt with meme-adjacent humor art direction"
    - "Every archetype has a prompt with character portrait art direction"
    - "Every death type has a prompt with dramatic darkly-comedic art direction"
    - "Image map provides typed Record mappings keyed by existing enums/types"
    - "All image paths follow /images/{category}/{id}.webp convention"
  artifacts:
    - path: "scripts/prompts/image-prompts.ts"
      provides: "Typed prompt library with ~28 ImagePrompt entries"
      exports: ["IMAGE_PROMPTS", "ImagePrompt"]
      min_lines: 150
    - path: "data/imageMap.ts"
      provides: "4 typed Record mappings (incidents, outcomes, archetypes, deaths)"
      exports: ["INCIDENT_IMAGES", "OUTCOME_IMAGES", "ARCHETYPE_IMAGES", "DEATH_IMAGES", "IncidentCategory", "OutcomeConsequenceType"]
      min_lines: 60
    - path: "data/index.ts"
      provides: "Updated barrel with imageMap exports"
      contains: "imageMap"
  key_links:
    - from: "scripts/prompts/image-prompts.ts"
      to: "data/imageMap.ts"
      via: "prompt IDs match image map filenames"
      pattern: "id:.*matches filename in path"
    - from: "data/imageMap.ts"
      to: "types.ts"
      via: "imports DeathType and ArchetypeId"
      pattern: "import.*DeathType|ArchetypeId.*from"
    - from: "data/index.ts"
      to: "data/imageMap.ts"
      via: "barrel export"
      pattern: "export.*from.*imageMap"
---

<objective>
Create the typed prompt library and image mapping config.

Purpose: Define all ~28 image generation prompts with art direction metadata, and create the runtime mapping from game entities to image paths. This makes the Wave 0 tests for prompts and image map pass (GREEN).

Output: scripts/prompts/image-prompts.ts (prompt library), data/imageMap.ts (entity-to-path mapping), updated data/index.ts barrel.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-image-asset-pipeline/13-CONTEXT.md
@.planning/phases/13-image-asset-pipeline/13-RESEARCH.md
@.planning/phases/13-image-asset-pipeline/13-00-SUMMARY.md

<interfaces>
<!-- Existing types the prompt library and image map must align with -->

From types.ts:
```typescript
export enum DeathType {
  BANKRUPT = "BANKRUPT",
  REPLACED_BY_SCRIPT = "REPLACED_BY_SCRIPT",
  CONGRESS = "CONGRESS",
  FLED_COUNTRY = "FLED_COUNTRY",
  PRISON = "PRISON",
  AUDIT_FAILURE = "AUDIT_FAILURE",
}

export type ArchetypeId =
  | "PRAGMATIST"
  | "SHADOW_ARCHITECT"
  | "DISRUPTOR"
  | "CONSERVATIVE"
  | "BALANCED"
  | "CHAOS_AGENT";
```

From data/index.ts (barrel — add imageMap exports):
```typescript
export { ARCHETYPES, ... } from "./archetypes";
export { DEATH_ENDINGS } from "./deathEndings";
// ADD: export { INCIDENT_IMAGES, OUTCOME_IMAGES, ARCHETYPE_IMAGES, DEATH_IMAGES } from "./imageMap";
```

Prompt art direction per CONTEXT.md decisions:
- Incidents: "Photorealistic with AI tells — hyper-realistic corporate photography where AI artifacts are the only clue"
- Outcomes: "Meme-adjacent humor — overtly funny, exaggerated expressions, absurd situations"
- Archetypes: "Character portraits — AI-generated portraits embodying each archetype personality"
- Deaths: "Dramatic + darkly comedic — over-the-top dramatic failure scenes with dark humor"

Safety filter guidance from RESEARCH.md:
- Frame all prompts as "satirical editorial illustration" or "corporate editorial cartoon"
- Add "fictional", "stylized", "editorial" language
- Avoid "prison", "handcuffed" — use "courthouse", "legal proceedings"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed prompt library with ~28 image prompts</name>
  <files>scripts/prompts/image-prompts.ts</files>
  <action>
    Create `scripts/prompts/image-prompts.ts` exporting `IMAGE_PROMPTS` array and `ImagePrompt` interface.

    The `ImagePrompt` interface:
    ```typescript
    export interface ImagePrompt {
      id: string;                    // Unique identifier (matches output filename, kebab-case)
      category: "incident" | "outcome" | "archetype" | "death";
      entityKey: string;             // Key in the image map (e.g., "BANKRUPT", "PRAGMATIST")
      prompt: string;                // The Gemini generation prompt
      negativePrompt?: string;       // What to avoid
      aspectRatio: "16:9" | "1:1" | "4:3" | "3:4" | "9:16";
      style: string;                 // Art direction category
    }
    ```

    Create prompts for all 4 categories following CONTEXT.md locked art styles:

    **6 Incident prompts** (aspectRatio: "16:9", style: "photorealistic-uncanny-valley"):
    - prompt-injection (entityKey: PROMPT_INJECTION)
    - model-drift (entityKey: MODEL_DRIFT)
    - explainability (entityKey: EXPLAINABILITY)
    - shadow-ai (entityKey: SHADOW_AI)
    - copyright (entityKey: COPYRIGHT)
    - general-dilemma (entityKey: GENERAL_DILEMMA)

    Art direction: Hyper-realistic corporate photography with subtle AI artifacts (extra fingers, warped text, impossible reflections). Frame as "editorial photograph for corporate magazine." Each should feel like an AI-generated stock photo that's almost right.

    **8 Outcome prompts** (aspectRatio: "4:3", style: "meme-adjacent-humor"):
    - budget-crater (entityKey: BUDGET_CRATER)
    - pr-disaster (entityKey: PR_DISASTER)
    - legal-hammer (entityKey: LEGAL_HAMMER)
    - team-revolt (entityKey: TEAM_REVOLT)
    - audit-doom (entityKey: AUDIT_DOOM)
    - career-over (entityKey: CAREER_OVER)
    - regulatory-nuke (entityKey: REGULATORY_NUKE)
    - data-breach (entityKey: DATA_BREACH)

    Art direction: Internet meme aesthetic — exaggerated expressions, absurd situations, sarcastic corporate humor. Frame as "satirical editorial cartoon, internet meme style." Think stock photo disasters played for laughs.

    **6 Archetype prompts** (aspectRatio: "1:1", style: "character-portrait"):
    - pragmatist (entityKey: PRAGMATIST)
    - shadow-architect (entityKey: SHADOW_ARCHITECT)
    - disruptor (entityKey: DISRUPTOR)
    - conservative (entityKey: CONSERVATIVE)
    - balanced (entityKey: BALANCED)
    - chaos-agent (entityKey: CHAOS_AGENT)

    Art direction: Professional character portraits embodying each archetype personality. LinkedIn-shareable quality. Frame as "futuristic corporate portrait, editorial magazine style." Pragmatist = calm composed executive, Shadow Architect = mysterious strategist, Disruptor = bold challenger, Conservative = cautious traditionalist, Balanced = wise diplomat, Chaos Agent = wild-eyed visionary.

    **6 Death prompts** (aspectRatio: "16:9", style: "dramatic-darkly-comedic"):
    - bankrupt (entityKey: BANKRUPT)
    - replaced-by-script (entityKey: REPLACED_BY_SCRIPT)
    - congress (entityKey: CONGRESS)
    - fled-country (entityKey: FLED_COUNTRY)
    - prison (entityKey: PRISON) — use "courthouse" / "legal proceedings" framing per safety filter guidance
    - audit-failure (entityKey: AUDIT_FAILURE)

    Art direction: Over-the-top dramatic disaster scenes with dark humor. "This is Fine" energy. Frame as "satirical editorial illustration, darkly comedic." Each should be a dramatic corporate disaster played for laughs.

    IMPORTANT: Every prompt must include "fictional", "stylized", or "editorial" language to avoid Gemini safety filter blocks (per RESEARCH.md pitfall 1).
  </action>
  <verify>
    <automated>bun vitest run tests/data/image-prompts.test.ts -x</automated>
  </verify>
  <done>scripts/prompts/image-prompts.ts exists with 26 typed prompts covering all categories. Prompt library tests pass (GREEN).</done>
</task>

<task type="auto">
  <name>Task 2: Create image map config and update barrel exports</name>
  <files>data/imageMap.ts, data/index.ts</files>
  <action>
    Create `data/imageMap.ts` following the project's established `Record<EnumType, T>` pattern (see archetypes.ts, deathEndings.ts for reference).

    Define two new type aliases in imageMap.ts (NOT in types.ts per CONTEXT.md — "No changes to Card interface or existing types"):

    ```typescript
    export type IncidentCategory =
      | "PROMPT_INJECTION"
      | "MODEL_DRIFT"
      | "EXPLAINABILITY"
      | "SHADOW_AI"
      | "COPYRIGHT"
      | "GENERAL_DILEMMA";

    export type OutcomeConsequenceType =
      | "BUDGET_CRATER"
      | "PR_DISASTER"
      | "LEGAL_HAMMER"
      | "TEAM_REVOLT"
      | "AUDIT_DOOM"
      | "CAREER_OVER"
      | "REGULATORY_NUKE"
      | "DATA_BREACH";
    ```

    Create 4 exported Record mappings:
    - `INCIDENT_IMAGES: Record<IncidentCategory, string>` — paths like `/images/incidents/prompt-injection.webp`
    - `OUTCOME_IMAGES: Record<OutcomeConsequenceType, string>` — paths like `/images/outcomes/budget-crater.webp`
    - `ARCHETYPE_IMAGES: Record<ArchetypeId, string>` — paths like `/images/archetypes/pragmatist.webp`
    - `DEATH_IMAGES: Record<DeathType, string>` — paths like `/images/deaths/bankrupt.webp`

    All paths must:
    - Start with `/images/` (no `public/` prefix — Vite serves public/ at root)
    - End with `.webp`
    - Use kebab-case filenames matching prompt IDs

    Import `DeathType` from `../types` and reference `ArchetypeId` type from `../types`.

    Update `data/index.ts` barrel to add:
    ```typescript
    export {
      INCIDENT_IMAGES,
      OUTCOME_IMAGES,
      ARCHETYPE_IMAGES,
      DEATH_IMAGES,
    } from "./imageMap";
    export type { IncidentCategory, OutcomeConsequenceType } from "./imageMap";
    ```
  </action>
  <verify>
    <automated>bun vitest run tests/data/image-map.test.ts -x && bun run typecheck</automated>
  </verify>
  <done>data/imageMap.ts exports 4 typed Record maps with correct paths. data/index.ts barrel updated. Image map tests pass (GREEN). TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `bun vitest run tests/data/image-prompts.test.ts -x` passes
- `bun vitest run tests/data/image-map.test.ts -x` passes
- `bun run typecheck` passes
- Prompt IDs match image map filenames exactly
- All paths follow /images/{category}/{id}.webp convention
</verification>

<success_criteria>
- 26 typed prompts in scripts/prompts/image-prompts.ts (6 incident + 8 outcome + 6 archetype + 6 death)
- 4 typed Record maps in data/imageMap.ts with complete enum coverage
- data/index.ts barrel updated with imageMap exports
- Wave 0 prompt and map tests pass GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/13-image-asset-pipeline/13-01-SUMMARY.md`
</output>
